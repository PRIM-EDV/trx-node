// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.3
// source: meshtastic/module_config.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { MemberRole, memberRoleFromJSON, memberRoleToJSON, Team, teamFromJSON, teamToJSON } from "./atak";
import { ChannelSettings } from "./channel";
import {
  Config_LoRaConfig_ModemPreset,
  config_LoRaConfig_ModemPresetFromJSON,
  config_LoRaConfig_ModemPresetToJSON,
  Config_LoRaConfig_RegionCode,
  config_LoRaConfig_RegionCodeFromJSON,
  config_LoRaConfig_RegionCodeToJSON,
} from "./config";

export enum RemoteHardwarePinType {
  /** UNKNOWN - Unset/unused */
  UNKNOWN = 0,
  /** DIGITAL_READ - GPIO pin can be read (if it is high / low) */
  DIGITAL_READ = 1,
  /** DIGITAL_WRITE - GPIO pin can be written to (high / low) */
  DIGITAL_WRITE = 2,
  UNRECOGNIZED = -1,
}

export function remoteHardwarePinTypeFromJSON(object: any): RemoteHardwarePinType {
  switch (object) {
    case 0:
    case "UNKNOWN":
      return RemoteHardwarePinType.UNKNOWN;
    case 1:
    case "DIGITAL_READ":
      return RemoteHardwarePinType.DIGITAL_READ;
    case 2:
    case "DIGITAL_WRITE":
      return RemoteHardwarePinType.DIGITAL_WRITE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RemoteHardwarePinType.UNRECOGNIZED;
  }
}

export function remoteHardwarePinTypeToJSON(object: RemoteHardwarePinType): string {
  switch (object) {
    case RemoteHardwarePinType.UNKNOWN:
      return "UNKNOWN";
    case RemoteHardwarePinType.DIGITAL_READ:
      return "DIGITAL_READ";
    case RemoteHardwarePinType.DIGITAL_WRITE:
      return "DIGITAL_WRITE";
    case RemoteHardwarePinType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Module Config */
export interface ModuleConfig {
  /** TODO: REPLACE */
  mqtt?:
    | ModuleConfig_MQTTConfig
    | undefined;
  /** TODO: REPLACE */
  serial?:
    | ModuleConfig_SerialConfig
    | undefined;
  /** TODO: REPLACE */
  externalNotification?:
    | ModuleConfig_ExternalNotificationConfig
    | undefined;
  /** TODO: REPLACE */
  storeForward?:
    | ModuleConfig_StoreForwardConfig
    | undefined;
  /** TODO: REPLACE */
  rangeTest?:
    | ModuleConfig_RangeTestConfig
    | undefined;
  /** TODO: REPLACE */
  telemetry?:
    | ModuleConfig_TelemetryConfig
    | undefined;
  /** TODO: REPLACE */
  cannedMessage?:
    | ModuleConfig_CannedMessageConfig
    | undefined;
  /** TODO: REPLACE */
  audio?:
    | ModuleConfig_AudioConfig
    | undefined;
  /** TODO: REPLACE */
  remoteHardware?:
    | ModuleConfig_RemoteHardwareConfig
    | undefined;
  /** TODO: REPLACE */
  neighborInfo?:
    | ModuleConfig_NeighborInfoConfig
    | undefined;
  /** TODO: REPLACE */
  ambientLighting?:
    | ModuleConfig_AmbientLightingConfig
    | undefined;
  /** TODO: REPLACE */
  detectionSensor?:
    | ModuleConfig_DetectionSensorConfig
    | undefined;
  /** TODO: REPLACE */
  paxcounter?:
    | ModuleConfig_PaxcounterConfig
    | undefined;
  /** TODO: REPLACE */
  statusmessage?:
    | ModuleConfig_StatusMessageConfig
    | undefined;
  /** Traffic management module config for mesh network optimization */
  trafficManagement?:
    | ModuleConfig_TrafficManagementConfig
    | undefined;
  /** TAK team/role configuration for TAK_TRACKER */
  tak?:
    | ModuleConfig_TAKConfig
    | undefined;
  /** MeshBeacon module config */
  meshBeacon?: ModuleConfig_MeshBeaconConfig | undefined;
}

/** MQTT Client Config */
export interface ModuleConfig_MQTTConfig {
  /**
   * If a meshtastic node is able to reach the internet it will normally attempt to gateway any channels that are marked as
   * is_uplink_enabled or is_downlink_enabled.
   */
  enabled: boolean;
  /**
   * The server to use for our MQTT global message gateway feature.
   * If not set, the default server will be used
   */
  address: string;
  /**
   * MQTT username to use (most useful for a custom MQTT server).
   * If using a custom server, this will be honoured even if empty.
   * If using the default server, this will only be honoured if set, otherwise the device will use the default username
   */
  username: string;
  /**
   * MQTT password to use (most useful for a custom MQTT server).
   * If using a custom server, this will be honoured even if empty.
   * If using the default server, this will only be honoured if set, otherwise the device will use the default password
   */
  password: string;
  /**
   * Whether to send encrypted or decrypted packets to MQTT.
   * This parameter is only honoured if you also set server
   * (the default official mqtt.meshtastic.org server can handle encrypted packets)
   * Decrypted packets may be useful for external systems that want to consume meshtastic packets
   */
  encryptionEnabled: boolean;
  /**
   * Deprecated: JSON packet support on MQTT was removed, and this field is ignored.
   *
   * @deprecated
   */
  jsonEnabled: boolean;
  /** If true, we attempt to establish a secure connection using TLS */
  tlsEnabled: boolean;
  /**
   * The root topic to use for MQTT messages. Default is "msh".
   * This is useful if you want to use a single MQTT server for multiple meshtastic networks and separate them via ACLs
   */
  root: string;
  /** If true, we can use the connected phone / client to proxy messages to MQTT instead of a direct connection */
  proxyToClientEnabled: boolean;
  /** If true, we will periodically report unencrypted information about our node to a map via MQTT */
  mapReportingEnabled: boolean;
  /** Settings for reporting information about our node to a map via MQTT */
  mapReportSettings: ModuleConfig_MapReportSettings | undefined;
}

/** Settings for reporting unencrypted information about our node to a map via MQTT */
export interface ModuleConfig_MapReportSettings {
  /** How often we should report our info to the map (in seconds) */
  publishIntervalSecs: number;
  /** Bits of precision for the location sent (default of 32 is full precision). */
  positionPrecision: number;
  /** Whether we have opted-in to report our location to the map */
  shouldReportLocation: boolean;
}

/** RemoteHardwareModule Config */
export interface ModuleConfig_RemoteHardwareConfig {
  /** Whether the Module is enabled */
  enabled: boolean;
  /** Whether the Module allows consumers to read / write to pins not defined in available_pins */
  allowUndefinedPinAccess: boolean;
  /** Exposes the available pins to the mesh for reading and writing */
  availablePins: RemoteHardwarePin[];
}

/** NeighborInfoModule Config */
export interface ModuleConfig_NeighborInfoConfig {
  /** Whether the Module is enabled */
  enabled: boolean;
  /**
   * Interval in seconds of how often we should try to send our
   * Neighbor Info (minimum is 14400, i.e., 4 hours)
   */
  updateInterval: number;
  /**
   * Whether in addition to sending it to MQTT and the PhoneAPI, our NeighborInfo should be transmitted over LoRa.
   * Note that this is not available on a channel with default key and name.
   */
  transmitOverLora: boolean;
}

/** Detection Sensor Module Config */
export interface ModuleConfig_DetectionSensorConfig {
  /** Whether the Module is enabled */
  enabled: boolean;
  /**
   * Interval in seconds of how often we can send a message to the mesh when a
   * trigger event is detected
   */
  minimumBroadcastSecs: number;
  /**
   * Interval in seconds of how often we should send a message to the mesh
   * with the current state regardless of trigger events When set to 0, only
   * trigger events will be broadcasted Works as a sort of status heartbeat
   * for peace of mind
   */
  stateBroadcastSecs: number;
  /**
   * Send ASCII bell with alert message
   * Useful for triggering ext. notification on bell
   */
  sendBell: boolean;
  /**
   * Friendly name used to format message sent to mesh
   * Example: A name "Motion" would result in a message "Motion detected"
   * Maximum length of 20 characters
   */
  name: string;
  /** GPIO pin to monitor for state changes */
  monitorPin: number;
  /** The type of trigger event to be used */
  detectionTriggerType: ModuleConfig_DetectionSensorConfig_TriggerType;
  /**
   * Whether or not use INPUT_PULLUP mode for GPIO pin
   * Only applicable if the board uses pull-up resistors on the pin
   */
  usePullup: boolean;
}

export enum ModuleConfig_DetectionSensorConfig_TriggerType {
  /** LOGIC_LOW - Event is triggered if pin is low */
  LOGIC_LOW = 0,
  /** LOGIC_HIGH - Event is triggered if pin is high */
  LOGIC_HIGH = 1,
  /** FALLING_EDGE - Event is triggered when pin goes high to low */
  FALLING_EDGE = 2,
  /** RISING_EDGE - Event is triggered when pin goes low to high */
  RISING_EDGE = 3,
  /**
   * EITHER_EDGE_ACTIVE_LOW - Event is triggered on every pin state change, low is considered to be
   * "active"
   */
  EITHER_EDGE_ACTIVE_LOW = 4,
  /**
   * EITHER_EDGE_ACTIVE_HIGH - Event is triggered on every pin state change, high is considered to be
   * "active"
   */
  EITHER_EDGE_ACTIVE_HIGH = 5,
  UNRECOGNIZED = -1,
}

export function moduleConfig_DetectionSensorConfig_TriggerTypeFromJSON(
  object: any,
): ModuleConfig_DetectionSensorConfig_TriggerType {
  switch (object) {
    case 0:
    case "LOGIC_LOW":
      return ModuleConfig_DetectionSensorConfig_TriggerType.LOGIC_LOW;
    case 1:
    case "LOGIC_HIGH":
      return ModuleConfig_DetectionSensorConfig_TriggerType.LOGIC_HIGH;
    case 2:
    case "FALLING_EDGE":
      return ModuleConfig_DetectionSensorConfig_TriggerType.FALLING_EDGE;
    case 3:
    case "RISING_EDGE":
      return ModuleConfig_DetectionSensorConfig_TriggerType.RISING_EDGE;
    case 4:
    case "EITHER_EDGE_ACTIVE_LOW":
      return ModuleConfig_DetectionSensorConfig_TriggerType.EITHER_EDGE_ACTIVE_LOW;
    case 5:
    case "EITHER_EDGE_ACTIVE_HIGH":
      return ModuleConfig_DetectionSensorConfig_TriggerType.EITHER_EDGE_ACTIVE_HIGH;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModuleConfig_DetectionSensorConfig_TriggerType.UNRECOGNIZED;
  }
}

export function moduleConfig_DetectionSensorConfig_TriggerTypeToJSON(
  object: ModuleConfig_DetectionSensorConfig_TriggerType,
): string {
  switch (object) {
    case ModuleConfig_DetectionSensorConfig_TriggerType.LOGIC_LOW:
      return "LOGIC_LOW";
    case ModuleConfig_DetectionSensorConfig_TriggerType.LOGIC_HIGH:
      return "LOGIC_HIGH";
    case ModuleConfig_DetectionSensorConfig_TriggerType.FALLING_EDGE:
      return "FALLING_EDGE";
    case ModuleConfig_DetectionSensorConfig_TriggerType.RISING_EDGE:
      return "RISING_EDGE";
    case ModuleConfig_DetectionSensorConfig_TriggerType.EITHER_EDGE_ACTIVE_LOW:
      return "EITHER_EDGE_ACTIVE_LOW";
    case ModuleConfig_DetectionSensorConfig_TriggerType.EITHER_EDGE_ACTIVE_HIGH:
      return "EITHER_EDGE_ACTIVE_HIGH";
    case ModuleConfig_DetectionSensorConfig_TriggerType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Audio Config for codec2 voice */
export interface ModuleConfig_AudioConfig {
  /** Whether Audio is enabled */
  codec2Enabled: boolean;
  /** PTT Pin */
  pttPin: number;
  /** The audio sample rate to use for codec2 */
  bitrate: ModuleConfig_AudioConfig_AudioBaud;
  /** I2S Word Select */
  i2sWs: number;
  /** I2S Data IN */
  i2sSd: number;
  /** I2S Data OUT */
  i2sDin: number;
  /** I2S Clock */
  i2sSck: number;
}

/** Baudrate for codec2 voice */
export enum ModuleConfig_AudioConfig_AudioBaud {
  CODEC2_DEFAULT = 0,
  CODEC2_3200 = 1,
  CODEC2_2400 = 2,
  CODEC2_1600 = 3,
  CODEC2_1400 = 4,
  CODEC2_1300 = 5,
  CODEC2_1200 = 6,
  CODEC2_700 = 7,
  CODEC2_700B = 8,
  UNRECOGNIZED = -1,
}

export function moduleConfig_AudioConfig_AudioBaudFromJSON(object: any): ModuleConfig_AudioConfig_AudioBaud {
  switch (object) {
    case 0:
    case "CODEC2_DEFAULT":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_DEFAULT;
    case 1:
    case "CODEC2_3200":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_3200;
    case 2:
    case "CODEC2_2400":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_2400;
    case 3:
    case "CODEC2_1600":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_1600;
    case 4:
    case "CODEC2_1400":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_1400;
    case 5:
    case "CODEC2_1300":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_1300;
    case 6:
    case "CODEC2_1200":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_1200;
    case 7:
    case "CODEC2_700":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_700;
    case 8:
    case "CODEC2_700B":
      return ModuleConfig_AudioConfig_AudioBaud.CODEC2_700B;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModuleConfig_AudioConfig_AudioBaud.UNRECOGNIZED;
  }
}

export function moduleConfig_AudioConfig_AudioBaudToJSON(object: ModuleConfig_AudioConfig_AudioBaud): string {
  switch (object) {
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_DEFAULT:
      return "CODEC2_DEFAULT";
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_3200:
      return "CODEC2_3200";
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_2400:
      return "CODEC2_2400";
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_1600:
      return "CODEC2_1600";
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_1400:
      return "CODEC2_1400";
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_1300:
      return "CODEC2_1300";
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_1200:
      return "CODEC2_1200";
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_700:
      return "CODEC2_700";
    case ModuleConfig_AudioConfig_AudioBaud.CODEC2_700B:
      return "CODEC2_700B";
    case ModuleConfig_AudioConfig_AudioBaud.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Config for the Paxcounter Module */
export interface ModuleConfig_PaxcounterConfig {
  /** Enable the Paxcounter Module */
  enabled: boolean;
  paxcounterUpdateInterval: number;
  /** WiFi RSSI threshold. Defaults to -80 */
  wifiThreshold: number;
  /** BLE RSSI threshold. Defaults to -80 */
  bleThreshold: number;
}

/**
 * Config for the Traffic Management module.
 * Provides packet inspection and traffic shaping to help reduce channel utilization
 */
export interface ModuleConfig_TrafficManagementConfig {
  /**
   * Minimum interval in seconds between position updates from the same node.
   * A non-zero value implicitly enables the suppression window; 0 disables it.
   */
  positionMinIntervalSecs: number;
  /**
   * Maximum hop distance from the requestor at which direct NodeInfo responses
   * are served from the local cache. A non-zero value implicitly enables direct
   * response; 0 disables it.
   */
  nodeinfoDirectResponseMaxHops: number;
  /**
   * Time window in seconds for per-node rate limiting.
   * A non-zero value implicitly enables rate limiting; 0 disables it.
   */
  rateLimitWindowSecs: number;
  /**
   * Maximum packets allowed per node within the rate limit window.
   * A non-zero value implicitly enables rate limiting; 0 disables it.
   */
  rateLimitMaxPackets: number;
  /**
   * Maximum unknown/undecryptable packets per rate window before the source
   * is dropped. A non-zero value implicitly enables unknown-packet filtering;
   * 0 disables it.
   */
  unknownPacketThreshold: number;
}

/** Serial Config */
export interface ModuleConfig_SerialConfig {
  /** Preferences for the SerialModule */
  enabled: boolean;
  /** TODO: REPLACE */
  echo: boolean;
  /** RX pin (should match Arduino gpio pin number) */
  rxd: number;
  /** TX pin (should match Arduino gpio pin number) */
  txd: number;
  /** Serial baud rate */
  baud: ModuleConfig_SerialConfig_SerialBaud;
  /** TODO: REPLACE */
  timeout: number;
  /** Mode for serial module operation */
  mode: ModuleConfig_SerialConfig_SerialMode;
  /**
   * Overrides the platform's defacto Serial port instance to use with Serial module config settings
   * This is currently only usable in output modes like NMEA / CalTopo and may behave strangely or not work at all in other modes
   * Existing logging over the Serial Console will still be present
   */
  overrideConsoleSerialPort: boolean;
}

/** TODO: REPLACE */
export enum ModuleConfig_SerialConfig_SerialBaud {
  BAUD_DEFAULT = 0,
  BAUD_110 = 1,
  BAUD_300 = 2,
  BAUD_600 = 3,
  BAUD_1200 = 4,
  BAUD_2400 = 5,
  BAUD_4800 = 6,
  BAUD_9600 = 7,
  BAUD_19200 = 8,
  BAUD_38400 = 9,
  BAUD_57600 = 10,
  BAUD_115200 = 11,
  BAUD_230400 = 12,
  BAUD_460800 = 13,
  BAUD_576000 = 14,
  BAUD_921600 = 15,
  UNRECOGNIZED = -1,
}

export function moduleConfig_SerialConfig_SerialBaudFromJSON(object: any): ModuleConfig_SerialConfig_SerialBaud {
  switch (object) {
    case 0:
    case "BAUD_DEFAULT":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_DEFAULT;
    case 1:
    case "BAUD_110":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_110;
    case 2:
    case "BAUD_300":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_300;
    case 3:
    case "BAUD_600":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_600;
    case 4:
    case "BAUD_1200":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_1200;
    case 5:
    case "BAUD_2400":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_2400;
    case 6:
    case "BAUD_4800":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_4800;
    case 7:
    case "BAUD_9600":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_9600;
    case 8:
    case "BAUD_19200":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_19200;
    case 9:
    case "BAUD_38400":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_38400;
    case 10:
    case "BAUD_57600":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_57600;
    case 11:
    case "BAUD_115200":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_115200;
    case 12:
    case "BAUD_230400":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_230400;
    case 13:
    case "BAUD_460800":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_460800;
    case 14:
    case "BAUD_576000":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_576000;
    case 15:
    case "BAUD_921600":
      return ModuleConfig_SerialConfig_SerialBaud.BAUD_921600;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModuleConfig_SerialConfig_SerialBaud.UNRECOGNIZED;
  }
}

export function moduleConfig_SerialConfig_SerialBaudToJSON(object: ModuleConfig_SerialConfig_SerialBaud): string {
  switch (object) {
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_DEFAULT:
      return "BAUD_DEFAULT";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_110:
      return "BAUD_110";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_300:
      return "BAUD_300";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_600:
      return "BAUD_600";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_1200:
      return "BAUD_1200";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_2400:
      return "BAUD_2400";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_4800:
      return "BAUD_4800";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_9600:
      return "BAUD_9600";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_19200:
      return "BAUD_19200";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_38400:
      return "BAUD_38400";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_57600:
      return "BAUD_57600";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_115200:
      return "BAUD_115200";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_230400:
      return "BAUD_230400";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_460800:
      return "BAUD_460800";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_576000:
      return "BAUD_576000";
    case ModuleConfig_SerialConfig_SerialBaud.BAUD_921600:
      return "BAUD_921600";
    case ModuleConfig_SerialConfig_SerialBaud.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** TODO: REPLACE */
export enum ModuleConfig_SerialConfig_SerialMode {
  DEFAULT = 0,
  SIMPLE = 1,
  PROTO = 2,
  TEXTMSG = 3,
  NMEA = 4,
  /** CALTOPO - NMEA messages specifically tailored for CalTopo */
  CALTOPO = 5,
  /** WS85 - Ecowitt WS85 weather station */
  WS85 = 6,
  /**
   * VE_DIRECT - VE.Direct is a serial protocol used by Victron Energy products
   * https://beta.ivc.no/wiki/index.php/Victron_VE_Direct_DIY_Cable
   */
  VE_DIRECT = 7,
  /**
   * MS_CONFIG - Used to configure and view some parameters of MeshSolar.
   * https://heltec.org/project/meshsolar/
   */
  MS_CONFIG = 8,
  /** LOG - Logs mesh traffic to the serial pins, ideal for logging via openLog or similar. */
  LOG = 9,
  /** LOGTEXT - only text (channel & DM) */
  LOGTEXT = 10,
  UNRECOGNIZED = -1,
}

export function moduleConfig_SerialConfig_SerialModeFromJSON(object: any): ModuleConfig_SerialConfig_SerialMode {
  switch (object) {
    case 0:
    case "DEFAULT":
      return ModuleConfig_SerialConfig_SerialMode.DEFAULT;
    case 1:
    case "SIMPLE":
      return ModuleConfig_SerialConfig_SerialMode.SIMPLE;
    case 2:
    case "PROTO":
      return ModuleConfig_SerialConfig_SerialMode.PROTO;
    case 3:
    case "TEXTMSG":
      return ModuleConfig_SerialConfig_SerialMode.TEXTMSG;
    case 4:
    case "NMEA":
      return ModuleConfig_SerialConfig_SerialMode.NMEA;
    case 5:
    case "CALTOPO":
      return ModuleConfig_SerialConfig_SerialMode.CALTOPO;
    case 6:
    case "WS85":
      return ModuleConfig_SerialConfig_SerialMode.WS85;
    case 7:
    case "VE_DIRECT":
      return ModuleConfig_SerialConfig_SerialMode.VE_DIRECT;
    case 8:
    case "MS_CONFIG":
      return ModuleConfig_SerialConfig_SerialMode.MS_CONFIG;
    case 9:
    case "LOG":
      return ModuleConfig_SerialConfig_SerialMode.LOG;
    case 10:
    case "LOGTEXT":
      return ModuleConfig_SerialConfig_SerialMode.LOGTEXT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModuleConfig_SerialConfig_SerialMode.UNRECOGNIZED;
  }
}

export function moduleConfig_SerialConfig_SerialModeToJSON(object: ModuleConfig_SerialConfig_SerialMode): string {
  switch (object) {
    case ModuleConfig_SerialConfig_SerialMode.DEFAULT:
      return "DEFAULT";
    case ModuleConfig_SerialConfig_SerialMode.SIMPLE:
      return "SIMPLE";
    case ModuleConfig_SerialConfig_SerialMode.PROTO:
      return "PROTO";
    case ModuleConfig_SerialConfig_SerialMode.TEXTMSG:
      return "TEXTMSG";
    case ModuleConfig_SerialConfig_SerialMode.NMEA:
      return "NMEA";
    case ModuleConfig_SerialConfig_SerialMode.CALTOPO:
      return "CALTOPO";
    case ModuleConfig_SerialConfig_SerialMode.WS85:
      return "WS85";
    case ModuleConfig_SerialConfig_SerialMode.VE_DIRECT:
      return "VE_DIRECT";
    case ModuleConfig_SerialConfig_SerialMode.MS_CONFIG:
      return "MS_CONFIG";
    case ModuleConfig_SerialConfig_SerialMode.LOG:
      return "LOG";
    case ModuleConfig_SerialConfig_SerialMode.LOGTEXT:
      return "LOGTEXT";
    case ModuleConfig_SerialConfig_SerialMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** External Notifications Config */
export interface ModuleConfig_ExternalNotificationConfig {
  /** Enable the ExternalNotificationModule */
  enabled: boolean;
  /**
   * When using in On/Off mode, keep the output on for this many
   * milliseconds. Default 1000ms (1 second).
   */
  outputMs: number;
  /**
   * Define the output pin GPIO setting Defaults to
   * EXT_NOTIFY_OUT if set for the board.
   * In standalone devices this pin should drive the LED to match the UI.
   */
  output: number;
  /**
   * Optional: Define a secondary output pin for a vibra motor
   * This is used in standalone devices to match the UI.
   */
  outputVibra: number;
  /**
   * Optional: Define a tertiary output pin for an active buzzer
   * This is used in standalone devices to to match the UI.
   */
  outputBuzzer: number;
  /**
   * IF this is true, the 'output' Pin will be pulled active high, false
   * means active low.
   */
  active: boolean;
  /** True: Alert when a text message arrives (output) */
  alertMessage: boolean;
  /** True: Alert when a text message arrives (output_vibra) */
  alertMessageVibra: boolean;
  /** True: Alert when a text message arrives (output_buzzer) */
  alertMessageBuzzer: boolean;
  /** True: Alert when the bell character is received (output) */
  alertBell: boolean;
  /** True: Alert when the bell character is received (output_vibra) */
  alertBellVibra: boolean;
  /** True: Alert when the bell character is received (output_buzzer) */
  alertBellBuzzer: boolean;
  /**
   * use a PWM output instead of a simple on/off output. This will ignore
   * the 'output', 'output_ms' and 'active' settings and use the
   * device.buzzer_gpio instead.
   */
  usePwm: boolean;
  /**
   * The notification will toggle with 'output_ms' for this time of seconds.
   * Default is 0 which means don't repeat at all. 60 would mean blink
   * and/or beep for 60 seconds
   */
  nagTimeout: number;
  /**
   * When true, enables devices with native I2S audio output to use the RTTTL over speaker like a buzzer
   * T-Watch S3 and T-Deck for example have this capability
   */
  useI2sAsBuzzer: boolean;
}

/** Store and Forward Module Config */
export interface ModuleConfig_StoreForwardConfig {
  /** Enable the Store and Forward Module */
  enabled: boolean;
  /** TODO: REPLACE */
  heartbeat: boolean;
  /** TODO: REPLACE */
  records: number;
  /** TODO: REPLACE */
  historyReturnMax: number;
  /** TODO: REPLACE */
  historyReturnWindow: number;
  /** Set to true to let this node act as a server that stores received messages and resends them upon request. */
  isServer: boolean;
}

/** Preferences for the RangeTestModule */
export interface ModuleConfig_RangeTestConfig {
  /** Enable the Range Test Module */
  enabled: boolean;
  /** Send out range test messages from this node */
  sender: number;
  /**
   * Bool value indicating that this node should save a RangeTest.csv file.
   * ESP32 Only
   */
  save: boolean;
  /**
   * Bool indicating that the node should cleanup / destroy it's RangeTest.csv file.
   * ESP32 Only
   */
  clearOnReboot: boolean;
}

/** Configuration for both device and environment metrics */
export interface ModuleConfig_TelemetryConfig {
  /**
   * Interval in seconds of how often we should try to send our
   * device metrics to the mesh
   */
  deviceUpdateInterval: number;
  environmentUpdateInterval: number;
  /**
   * Preferences for the Telemetry Module (Environment)
   * Enable/Disable the telemetry measurement module measurement collection
   */
  environmentMeasurementEnabled: boolean;
  /** Enable/Disable the telemetry measurement module on-device display */
  environmentScreenEnabled: boolean;
  /**
   * We'll always read the sensor in Celsius, but sometimes we might want to
   * display the results in Fahrenheit as a "user preference".
   */
  environmentDisplayFahrenheit: boolean;
  /** Enable/Disable the air quality metrics */
  airQualityEnabled: boolean;
  /**
   * Interval in seconds of how often we should try to send our
   * air quality metrics to the mesh
   */
  airQualityInterval: number;
  /** Enable/disable Power metrics */
  powerMeasurementEnabled: boolean;
  /**
   * Interval in seconds of how often we should try to send our
   * power metrics to the mesh
   */
  powerUpdateInterval: number;
  /** Enable/Disable the power measurement module on-device display */
  powerScreenEnabled: boolean;
  /**
   * Preferences for the (Health) Telemetry Module
   * Enable/Disable the telemetry measurement module measurement collection
   */
  healthMeasurementEnabled: boolean;
  /**
   * Interval in seconds of how often we should try to send our
   * health metrics to the mesh
   */
  healthUpdateInterval: number;
  /** Enable/Disable the health telemetry module on-device display */
  healthScreenEnabled: boolean;
  /**
   * Enable/Disable the device telemetry module to send metrics to the mesh
   * Note: We will still send telemtry to the connected phone / client every minute over the API
   */
  deviceTelemetryEnabled: boolean;
  /** Enable/Disable the air quality telemetry measurement module on-device display */
  airQualityScreenEnabled: boolean;
}

/** Canned Messages Module Config */
export interface ModuleConfig_CannedMessageConfig {
  /** Enable the rotary encoder #1. This is a 'dumb' encoder sending pulses on both A and B pins while rotating. */
  rotary1Enabled: boolean;
  /** GPIO pin for rotary encoder A port. */
  inputbrokerPinA: number;
  /** GPIO pin for rotary encoder B port. */
  inputbrokerPinB: number;
  /** GPIO pin for rotary encoder Press port. */
  inputbrokerPinPress: number;
  /** Generate input event on CW of this kind. */
  inputbrokerEventCw: ModuleConfig_CannedMessageConfig_InputEventChar;
  /** Generate input event on CCW of this kind. */
  inputbrokerEventCcw: ModuleConfig_CannedMessageConfig_InputEventChar;
  /** Generate input event on Press of this kind. */
  inputbrokerEventPress: ModuleConfig_CannedMessageConfig_InputEventChar;
  /** Enable the Up/Down/Select input device. Can be RAK rotary encoder or 3 buttons. Uses the a/b/press definitions from inputbroker. */
  updown1Enabled: boolean;
  /**
   * Enable/disable CannedMessageModule.
   *
   * @deprecated
   */
  enabled: boolean;
  /**
   * Input event origin accepted by the canned message module.
   * Can be e.g. "rotEnc1", "upDownEnc1", "scanAndSelect", "cardkb", "serialkb", or keyword "_any"
   *
   * @deprecated
   */
  allowInputSource: string;
  /**
   * CannedMessageModule also sends a bell character with the messages.
   * ExternalNotificationModule can benefit from this feature.
   */
  sendBell: boolean;
}

/** TODO: REPLACE */
export enum ModuleConfig_CannedMessageConfig_InputEventChar {
  /** NONE - TODO: REPLACE */
  NONE = 0,
  /** UP - TODO: REPLACE */
  UP = 17,
  /** DOWN - TODO: REPLACE */
  DOWN = 18,
  /** LEFT - TODO: REPLACE */
  LEFT = 19,
  /** RIGHT - TODO: REPLACE */
  RIGHT = 20,
  /** SELECT - '\n' */
  SELECT = 10,
  /** BACK - TODO: REPLACE */
  BACK = 27,
  /** CANCEL - TODO: REPLACE */
  CANCEL = 24,
  UNRECOGNIZED = -1,
}

export function moduleConfig_CannedMessageConfig_InputEventCharFromJSON(
  object: any,
): ModuleConfig_CannedMessageConfig_InputEventChar {
  switch (object) {
    case 0:
    case "NONE":
      return ModuleConfig_CannedMessageConfig_InputEventChar.NONE;
    case 17:
    case "UP":
      return ModuleConfig_CannedMessageConfig_InputEventChar.UP;
    case 18:
    case "DOWN":
      return ModuleConfig_CannedMessageConfig_InputEventChar.DOWN;
    case 19:
    case "LEFT":
      return ModuleConfig_CannedMessageConfig_InputEventChar.LEFT;
    case 20:
    case "RIGHT":
      return ModuleConfig_CannedMessageConfig_InputEventChar.RIGHT;
    case 10:
    case "SELECT":
      return ModuleConfig_CannedMessageConfig_InputEventChar.SELECT;
    case 27:
    case "BACK":
      return ModuleConfig_CannedMessageConfig_InputEventChar.BACK;
    case 24:
    case "CANCEL":
      return ModuleConfig_CannedMessageConfig_InputEventChar.CANCEL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModuleConfig_CannedMessageConfig_InputEventChar.UNRECOGNIZED;
  }
}

export function moduleConfig_CannedMessageConfig_InputEventCharToJSON(
  object: ModuleConfig_CannedMessageConfig_InputEventChar,
): string {
  switch (object) {
    case ModuleConfig_CannedMessageConfig_InputEventChar.NONE:
      return "NONE";
    case ModuleConfig_CannedMessageConfig_InputEventChar.UP:
      return "UP";
    case ModuleConfig_CannedMessageConfig_InputEventChar.DOWN:
      return "DOWN";
    case ModuleConfig_CannedMessageConfig_InputEventChar.LEFT:
      return "LEFT";
    case ModuleConfig_CannedMessageConfig_InputEventChar.RIGHT:
      return "RIGHT";
    case ModuleConfig_CannedMessageConfig_InputEventChar.SELECT:
      return "SELECT";
    case ModuleConfig_CannedMessageConfig_InputEventChar.BACK:
      return "BACK";
    case ModuleConfig_CannedMessageConfig_InputEventChar.CANCEL:
      return "CANCEL";
    case ModuleConfig_CannedMessageConfig_InputEventChar.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Ambient Lighting Module - Settings for control of onboard LEDs to allow users to adjust the brightness levels and respective color levels.
 * Initially created for the RAK14001 RGB LED module.
 */
export interface ModuleConfig_AmbientLightingConfig {
  /** Sets LED to on or off. */
  ledState: boolean;
  /** Sets the current for the LED output. Default is 10. */
  current: number;
  /** Sets the red LED level. Values are 0-255. */
  red: number;
  /** Sets the green LED level. Values are 0-255. */
  green: number;
  /** Sets the blue LED level. Values are 0-255. */
  blue: number;
}

/** StatusMessage config - Allows setting a status message for a node to periodically rebroadcast */
export interface ModuleConfig_StatusMessageConfig {
  /** The actual status string */
  nodeStatus: string;
}

/** MeshBeacon module config */
export interface ModuleConfig_MeshBeaconConfig {
  /** Bitwise-OR of Flags values (listen / broadcast / legacy-split toggles). */
  flags: number;
  /**
   * Optional: node ID to send beacon messages AS.
   * When set, the `from` field of outgoing beacon packets is set to this node ID,
   * making beacons appear to originate from that node.
   * When unset (0), beacons are sent as the local node.
   * A remote admin can only set this field to their own node ID.
   */
  broadcastSendAsNode: number;
  /** Message to include in each beacon broadcast. Max 100 bytes enforced by firmware. */
  broadcastMessage: string;
  /** Optional channel (name + PSK) to advertise in the MeshBeacon offer_channel field. */
  broadcastOfferChannel:
    | ChannelSettings
    | undefined;
  /** Optional region to advertise in the MeshBeacon offer_region field. */
  broadcastOfferRegion: Config_LoRaConfig_RegionCode;
  /** Optional modem preset to advertise in the MeshBeacon offer_preset field. */
  broadcastOfferPreset?:
    | Config_LoRaConfig_ModemPreset
    | undefined;
  /**
   * Single-target TX channel: channel settings (name + PSK) to send beacons on.
   * If unset, beacons go out on the primary channel. Used only when broadcast_targets is empty.
   * NOTE: the single-target path embeds the ChannelSettings inline here, whereas a
   * broadcast_targets entry references a channel-table slot by channel_index instead — see
   * BroadcastTarget. The two paths are equal, first-class options; only this representation differs.
   */
  broadcastOnChannel:
    | ChannelSettings
    | undefined;
  /** Region to use when sending beacons on broadcast_on_preset. */
  broadcastOnRegion: Config_LoRaConfig_RegionCode;
  /**
   * Modem preset to use when sending beacons.
   * If different from current config, the radio is temporarily switched for TX.
   */
  broadcastOnPreset?:
    | Config_LoRaConfig_ModemPreset
    | undefined;
  /** How often to broadcast, in seconds. Min 3600 (1 h), default 3600. */
  broadcastIntervalSecs: number;
  /**
   * Multi-target broadcast list.
   * When non-empty the broadcaster transmits one beacon copy per entry in sequence,
   * each temporarily switching the radio to that entry's preset/region/channel.
   * When empty, the broadcaster uses the scalar broadcast_on_preset / broadcast_on_region /
   * broadcast_on_channel fields instead (the single-target path).
   * Single- and multi-target are equal, first-class options — neither is preferred or
   * deprecated. They differ only in how the TX channel is named: broadcast_on_channel embeds a
   * ChannelSettings inline, while a target references an existing channel-table slot by
   * channel_index (see BroadcastTarget).
   */
  broadcastTargets: ModuleConfig_MeshBeaconConfig_BroadcastTarget[];
}

/**
 * Boolean options for the beacon module, packed into the `flags` bitfield below.
 * OR the FLAG_* values together; a flag is on when its bit is set.
 */
export enum ModuleConfig_MeshBeaconConfig_Flags {
  /** FLAG_NONE - No options enabled. */
  FLAG_NONE = 0,
  /**
   * FLAG_LISTEN_ENABLED - Enable receiving MESH_BEACON_APP packets from other nodes.
   * The text portion is delivered to the local message inbox.
   * Offered channel/preset are stored for the client app to act on.
   */
  FLAG_LISTEN_ENABLED = 1,
  /** FLAG_BROADCAST_ENABLED - Enable periodically broadcasting MESH_BEACON_APP packets from this node. */
  FLAG_BROADCAST_ENABLED = 2,
  /**
   * FLAG_LEGACY_SPLIT - When both text and offer content are present, split the beacon into a separate
   * MESH_BEACON_APP (offer only) and TEXT_MESSAGE_APP (text only) packet, so firmware
   * that only decodes TEXT_MESSAGE_APP still receives the human-readable text.
   */
  FLAG_LEGACY_SPLIT = 4,
  UNRECOGNIZED = -1,
}

export function moduleConfig_MeshBeaconConfig_FlagsFromJSON(object: any): ModuleConfig_MeshBeaconConfig_Flags {
  switch (object) {
    case 0:
    case "FLAG_NONE":
      return ModuleConfig_MeshBeaconConfig_Flags.FLAG_NONE;
    case 1:
    case "FLAG_LISTEN_ENABLED":
      return ModuleConfig_MeshBeaconConfig_Flags.FLAG_LISTEN_ENABLED;
    case 2:
    case "FLAG_BROADCAST_ENABLED":
      return ModuleConfig_MeshBeaconConfig_Flags.FLAG_BROADCAST_ENABLED;
    case 4:
    case "FLAG_LEGACY_SPLIT":
      return ModuleConfig_MeshBeaconConfig_Flags.FLAG_LEGACY_SPLIT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModuleConfig_MeshBeaconConfig_Flags.UNRECOGNIZED;
  }
}

export function moduleConfig_MeshBeaconConfig_FlagsToJSON(object: ModuleConfig_MeshBeaconConfig_Flags): string {
  switch (object) {
    case ModuleConfig_MeshBeaconConfig_Flags.FLAG_NONE:
      return "FLAG_NONE";
    case ModuleConfig_MeshBeaconConfig_Flags.FLAG_LISTEN_ENABLED:
      return "FLAG_LISTEN_ENABLED";
    case ModuleConfig_MeshBeaconConfig_Flags.FLAG_BROADCAST_ENABLED:
      return "FLAG_BROADCAST_ENABLED";
    case ModuleConfig_MeshBeaconConfig_Flags.FLAG_LEGACY_SPLIT:
      return "FLAG_LEGACY_SPLIT";
    case ModuleConfig_MeshBeaconConfig_Flags.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * One entry in the multi-target broadcast list.
 * The broadcaster transmits one beacon copy per entry, each on its own radio settings.
 */
export interface ModuleConfig_MeshBeaconConfig_BroadcastTarget {
  /**
   * Modem preset to use for this target.
   * Falls back to the running config preset if unset.
   */
  preset?:
    | Config_LoRaConfig_ModemPreset
    | undefined;
  /** Region to use for this target. UNSET means use the running config region. */
  region: Config_LoRaConfig_RegionCode;
  /**
   * Index into the device's channel table (0..MAX_NUM_CHANNELS-1) of the channel to
   * transmit this target's beacon on. The referenced channel must already be configured
   * on the node (its key is needed to encrypt). If unset, the default channel for the
   * preset is used.
   */
  channelIndex?: number | undefined;
}

/** TAK team/role configuration */
export interface ModuleConfig_TAKConfig {
  /**
   * Team color.
   * Default Unspecifed_Color -> firmware uses Cyan
   */
  team: Team;
  /**
   * Member role.
   * Default Unspecifed -> firmware uses TeamMember
   */
  role: MemberRole;
}

/** A GPIO pin definition for remote hardware module */
export interface RemoteHardwarePin {
  /** GPIO Pin number (must match Arduino) */
  gpioPin: number;
  /** Name for the GPIO pin (i.e. Front gate, mailbox, etc) */
  name: string;
  /** Type of GPIO access available to consumers on the mesh */
  type: RemoteHardwarePinType;
}

function createBaseModuleConfig(): ModuleConfig {
  return {
    mqtt: undefined,
    serial: undefined,
    externalNotification: undefined,
    storeForward: undefined,
    rangeTest: undefined,
    telemetry: undefined,
    cannedMessage: undefined,
    audio: undefined,
    remoteHardware: undefined,
    neighborInfo: undefined,
    ambientLighting: undefined,
    detectionSensor: undefined,
    paxcounter: undefined,
    statusmessage: undefined,
    trafficManagement: undefined,
    tak: undefined,
    meshBeacon: undefined,
  };
}

export const ModuleConfig = {
  encode(message: ModuleConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.mqtt !== undefined) {
      ModuleConfig_MQTTConfig.encode(message.mqtt, writer.uint32(10).fork()).ldelim();
    }
    if (message.serial !== undefined) {
      ModuleConfig_SerialConfig.encode(message.serial, writer.uint32(18).fork()).ldelim();
    }
    if (message.externalNotification !== undefined) {
      ModuleConfig_ExternalNotificationConfig.encode(message.externalNotification, writer.uint32(26).fork()).ldelim();
    }
    if (message.storeForward !== undefined) {
      ModuleConfig_StoreForwardConfig.encode(message.storeForward, writer.uint32(34).fork()).ldelim();
    }
    if (message.rangeTest !== undefined) {
      ModuleConfig_RangeTestConfig.encode(message.rangeTest, writer.uint32(42).fork()).ldelim();
    }
    if (message.telemetry !== undefined) {
      ModuleConfig_TelemetryConfig.encode(message.telemetry, writer.uint32(50).fork()).ldelim();
    }
    if (message.cannedMessage !== undefined) {
      ModuleConfig_CannedMessageConfig.encode(message.cannedMessage, writer.uint32(58).fork()).ldelim();
    }
    if (message.audio !== undefined) {
      ModuleConfig_AudioConfig.encode(message.audio, writer.uint32(66).fork()).ldelim();
    }
    if (message.remoteHardware !== undefined) {
      ModuleConfig_RemoteHardwareConfig.encode(message.remoteHardware, writer.uint32(74).fork()).ldelim();
    }
    if (message.neighborInfo !== undefined) {
      ModuleConfig_NeighborInfoConfig.encode(message.neighborInfo, writer.uint32(82).fork()).ldelim();
    }
    if (message.ambientLighting !== undefined) {
      ModuleConfig_AmbientLightingConfig.encode(message.ambientLighting, writer.uint32(90).fork()).ldelim();
    }
    if (message.detectionSensor !== undefined) {
      ModuleConfig_DetectionSensorConfig.encode(message.detectionSensor, writer.uint32(98).fork()).ldelim();
    }
    if (message.paxcounter !== undefined) {
      ModuleConfig_PaxcounterConfig.encode(message.paxcounter, writer.uint32(106).fork()).ldelim();
    }
    if (message.statusmessage !== undefined) {
      ModuleConfig_StatusMessageConfig.encode(message.statusmessage, writer.uint32(114).fork()).ldelim();
    }
    if (message.trafficManagement !== undefined) {
      ModuleConfig_TrafficManagementConfig.encode(message.trafficManagement, writer.uint32(122).fork()).ldelim();
    }
    if (message.tak !== undefined) {
      ModuleConfig_TAKConfig.encode(message.tak, writer.uint32(130).fork()).ldelim();
    }
    if (message.meshBeacon !== undefined) {
      ModuleConfig_MeshBeaconConfig.encode(message.meshBeacon, writer.uint32(138).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.mqtt = ModuleConfig_MQTTConfig.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.serial = ModuleConfig_SerialConfig.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.externalNotification = ModuleConfig_ExternalNotificationConfig.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.storeForward = ModuleConfig_StoreForwardConfig.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.rangeTest = ModuleConfig_RangeTestConfig.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.telemetry = ModuleConfig_TelemetryConfig.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.cannedMessage = ModuleConfig_CannedMessageConfig.decode(reader, reader.uint32());
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.audio = ModuleConfig_AudioConfig.decode(reader, reader.uint32());
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.remoteHardware = ModuleConfig_RemoteHardwareConfig.decode(reader, reader.uint32());
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.neighborInfo = ModuleConfig_NeighborInfoConfig.decode(reader, reader.uint32());
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.ambientLighting = ModuleConfig_AmbientLightingConfig.decode(reader, reader.uint32());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.detectionSensor = ModuleConfig_DetectionSensorConfig.decode(reader, reader.uint32());
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.paxcounter = ModuleConfig_PaxcounterConfig.decode(reader, reader.uint32());
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.statusmessage = ModuleConfig_StatusMessageConfig.decode(reader, reader.uint32());
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.trafficManagement = ModuleConfig_TrafficManagementConfig.decode(reader, reader.uint32());
          continue;
        case 16:
          if (tag !== 130) {
            break;
          }

          message.tak = ModuleConfig_TAKConfig.decode(reader, reader.uint32());
          continue;
        case 17:
          if (tag !== 138) {
            break;
          }

          message.meshBeacon = ModuleConfig_MeshBeaconConfig.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig {
    return {
      mqtt: isSet(object.mqtt) ? ModuleConfig_MQTTConfig.fromJSON(object.mqtt) : undefined,
      serial: isSet(object.serial) ? ModuleConfig_SerialConfig.fromJSON(object.serial) : undefined,
      externalNotification: isSet(object.externalNotification)
        ? ModuleConfig_ExternalNotificationConfig.fromJSON(object.externalNotification)
        : undefined,
      storeForward: isSet(object.storeForward)
        ? ModuleConfig_StoreForwardConfig.fromJSON(object.storeForward)
        : undefined,
      rangeTest: isSet(object.rangeTest) ? ModuleConfig_RangeTestConfig.fromJSON(object.rangeTest) : undefined,
      telemetry: isSet(object.telemetry) ? ModuleConfig_TelemetryConfig.fromJSON(object.telemetry) : undefined,
      cannedMessage: isSet(object.cannedMessage)
        ? ModuleConfig_CannedMessageConfig.fromJSON(object.cannedMessage)
        : undefined,
      audio: isSet(object.audio) ? ModuleConfig_AudioConfig.fromJSON(object.audio) : undefined,
      remoteHardware: isSet(object.remoteHardware)
        ? ModuleConfig_RemoteHardwareConfig.fromJSON(object.remoteHardware)
        : undefined,
      neighborInfo: isSet(object.neighborInfo)
        ? ModuleConfig_NeighborInfoConfig.fromJSON(object.neighborInfo)
        : undefined,
      ambientLighting: isSet(object.ambientLighting)
        ? ModuleConfig_AmbientLightingConfig.fromJSON(object.ambientLighting)
        : undefined,
      detectionSensor: isSet(object.detectionSensor)
        ? ModuleConfig_DetectionSensorConfig.fromJSON(object.detectionSensor)
        : undefined,
      paxcounter: isSet(object.paxcounter) ? ModuleConfig_PaxcounterConfig.fromJSON(object.paxcounter) : undefined,
      statusmessage: isSet(object.statusmessage)
        ? ModuleConfig_StatusMessageConfig.fromJSON(object.statusmessage)
        : undefined,
      trafficManagement: isSet(object.trafficManagement)
        ? ModuleConfig_TrafficManagementConfig.fromJSON(object.trafficManagement)
        : undefined,
      tak: isSet(object.tak) ? ModuleConfig_TAKConfig.fromJSON(object.tak) : undefined,
      meshBeacon: isSet(object.meshBeacon) ? ModuleConfig_MeshBeaconConfig.fromJSON(object.meshBeacon) : undefined,
    };
  },

  toJSON(message: ModuleConfig): unknown {
    const obj: any = {};
    if (message.mqtt !== undefined) {
      obj.mqtt = ModuleConfig_MQTTConfig.toJSON(message.mqtt);
    }
    if (message.serial !== undefined) {
      obj.serial = ModuleConfig_SerialConfig.toJSON(message.serial);
    }
    if (message.externalNotification !== undefined) {
      obj.externalNotification = ModuleConfig_ExternalNotificationConfig.toJSON(message.externalNotification);
    }
    if (message.storeForward !== undefined) {
      obj.storeForward = ModuleConfig_StoreForwardConfig.toJSON(message.storeForward);
    }
    if (message.rangeTest !== undefined) {
      obj.rangeTest = ModuleConfig_RangeTestConfig.toJSON(message.rangeTest);
    }
    if (message.telemetry !== undefined) {
      obj.telemetry = ModuleConfig_TelemetryConfig.toJSON(message.telemetry);
    }
    if (message.cannedMessage !== undefined) {
      obj.cannedMessage = ModuleConfig_CannedMessageConfig.toJSON(message.cannedMessage);
    }
    if (message.audio !== undefined) {
      obj.audio = ModuleConfig_AudioConfig.toJSON(message.audio);
    }
    if (message.remoteHardware !== undefined) {
      obj.remoteHardware = ModuleConfig_RemoteHardwareConfig.toJSON(message.remoteHardware);
    }
    if (message.neighborInfo !== undefined) {
      obj.neighborInfo = ModuleConfig_NeighborInfoConfig.toJSON(message.neighborInfo);
    }
    if (message.ambientLighting !== undefined) {
      obj.ambientLighting = ModuleConfig_AmbientLightingConfig.toJSON(message.ambientLighting);
    }
    if (message.detectionSensor !== undefined) {
      obj.detectionSensor = ModuleConfig_DetectionSensorConfig.toJSON(message.detectionSensor);
    }
    if (message.paxcounter !== undefined) {
      obj.paxcounter = ModuleConfig_PaxcounterConfig.toJSON(message.paxcounter);
    }
    if (message.statusmessage !== undefined) {
      obj.statusmessage = ModuleConfig_StatusMessageConfig.toJSON(message.statusmessage);
    }
    if (message.trafficManagement !== undefined) {
      obj.trafficManagement = ModuleConfig_TrafficManagementConfig.toJSON(message.trafficManagement);
    }
    if (message.tak !== undefined) {
      obj.tak = ModuleConfig_TAKConfig.toJSON(message.tak);
    }
    if (message.meshBeacon !== undefined) {
      obj.meshBeacon = ModuleConfig_MeshBeaconConfig.toJSON(message.meshBeacon);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig>, I>>(base?: I): ModuleConfig {
    return ModuleConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig>, I>>(object: I): ModuleConfig {
    const message = createBaseModuleConfig();
    message.mqtt = (object.mqtt !== undefined && object.mqtt !== null)
      ? ModuleConfig_MQTTConfig.fromPartial(object.mqtt)
      : undefined;
    message.serial = (object.serial !== undefined && object.serial !== null)
      ? ModuleConfig_SerialConfig.fromPartial(object.serial)
      : undefined;
    message.externalNotification = (object.externalNotification !== undefined && object.externalNotification !== null)
      ? ModuleConfig_ExternalNotificationConfig.fromPartial(object.externalNotification)
      : undefined;
    message.storeForward = (object.storeForward !== undefined && object.storeForward !== null)
      ? ModuleConfig_StoreForwardConfig.fromPartial(object.storeForward)
      : undefined;
    message.rangeTest = (object.rangeTest !== undefined && object.rangeTest !== null)
      ? ModuleConfig_RangeTestConfig.fromPartial(object.rangeTest)
      : undefined;
    message.telemetry = (object.telemetry !== undefined && object.telemetry !== null)
      ? ModuleConfig_TelemetryConfig.fromPartial(object.telemetry)
      : undefined;
    message.cannedMessage = (object.cannedMessage !== undefined && object.cannedMessage !== null)
      ? ModuleConfig_CannedMessageConfig.fromPartial(object.cannedMessage)
      : undefined;
    message.audio = (object.audio !== undefined && object.audio !== null)
      ? ModuleConfig_AudioConfig.fromPartial(object.audio)
      : undefined;
    message.remoteHardware = (object.remoteHardware !== undefined && object.remoteHardware !== null)
      ? ModuleConfig_RemoteHardwareConfig.fromPartial(object.remoteHardware)
      : undefined;
    message.neighborInfo = (object.neighborInfo !== undefined && object.neighborInfo !== null)
      ? ModuleConfig_NeighborInfoConfig.fromPartial(object.neighborInfo)
      : undefined;
    message.ambientLighting = (object.ambientLighting !== undefined && object.ambientLighting !== null)
      ? ModuleConfig_AmbientLightingConfig.fromPartial(object.ambientLighting)
      : undefined;
    message.detectionSensor = (object.detectionSensor !== undefined && object.detectionSensor !== null)
      ? ModuleConfig_DetectionSensorConfig.fromPartial(object.detectionSensor)
      : undefined;
    message.paxcounter = (object.paxcounter !== undefined && object.paxcounter !== null)
      ? ModuleConfig_PaxcounterConfig.fromPartial(object.paxcounter)
      : undefined;
    message.statusmessage = (object.statusmessage !== undefined && object.statusmessage !== null)
      ? ModuleConfig_StatusMessageConfig.fromPartial(object.statusmessage)
      : undefined;
    message.trafficManagement = (object.trafficManagement !== undefined && object.trafficManagement !== null)
      ? ModuleConfig_TrafficManagementConfig.fromPartial(object.trafficManagement)
      : undefined;
    message.tak = (object.tak !== undefined && object.tak !== null)
      ? ModuleConfig_TAKConfig.fromPartial(object.tak)
      : undefined;
    message.meshBeacon = (object.meshBeacon !== undefined && object.meshBeacon !== null)
      ? ModuleConfig_MeshBeaconConfig.fromPartial(object.meshBeacon)
      : undefined;
    return message;
  },
};

function createBaseModuleConfig_MQTTConfig(): ModuleConfig_MQTTConfig {
  return {
    enabled: false,
    address: "",
    username: "",
    password: "",
    encryptionEnabled: false,
    jsonEnabled: false,
    tlsEnabled: false,
    root: "",
    proxyToClientEnabled: false,
    mapReportingEnabled: false,
    mapReportSettings: undefined,
  };
}

export const ModuleConfig_MQTTConfig = {
  encode(message: ModuleConfig_MQTTConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.username !== "") {
      writer.uint32(26).string(message.username);
    }
    if (message.password !== "") {
      writer.uint32(34).string(message.password);
    }
    if (message.encryptionEnabled !== false) {
      writer.uint32(40).bool(message.encryptionEnabled);
    }
    if (message.jsonEnabled !== false) {
      writer.uint32(48).bool(message.jsonEnabled);
    }
    if (message.tlsEnabled !== false) {
      writer.uint32(56).bool(message.tlsEnabled);
    }
    if (message.root !== "") {
      writer.uint32(66).string(message.root);
    }
    if (message.proxyToClientEnabled !== false) {
      writer.uint32(72).bool(message.proxyToClientEnabled);
    }
    if (message.mapReportingEnabled !== false) {
      writer.uint32(80).bool(message.mapReportingEnabled);
    }
    if (message.mapReportSettings !== undefined) {
      ModuleConfig_MapReportSettings.encode(message.mapReportSettings, writer.uint32(90).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_MQTTConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_MQTTConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.address = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.username = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.password = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.encryptionEnabled = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.jsonEnabled = reader.bool();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.tlsEnabled = reader.bool();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.root = reader.string();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.proxyToClientEnabled = reader.bool();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.mapReportingEnabled = reader.bool();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.mapReportSettings = ModuleConfig_MapReportSettings.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_MQTTConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      encryptionEnabled: isSet(object.encryptionEnabled) ? globalThis.Boolean(object.encryptionEnabled) : false,
      jsonEnabled: isSet(object.jsonEnabled) ? globalThis.Boolean(object.jsonEnabled) : false,
      tlsEnabled: isSet(object.tlsEnabled) ? globalThis.Boolean(object.tlsEnabled) : false,
      root: isSet(object.root) ? globalThis.String(object.root) : "",
      proxyToClientEnabled: isSet(object.proxyToClientEnabled)
        ? globalThis.Boolean(object.proxyToClientEnabled)
        : false,
      mapReportingEnabled: isSet(object.mapReportingEnabled) ? globalThis.Boolean(object.mapReportingEnabled) : false,
      mapReportSettings: isSet(object.mapReportSettings)
        ? ModuleConfig_MapReportSettings.fromJSON(object.mapReportSettings)
        : undefined,
    };
  },

  toJSON(message: ModuleConfig_MQTTConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.encryptionEnabled !== false) {
      obj.encryptionEnabled = message.encryptionEnabled;
    }
    if (message.jsonEnabled !== false) {
      obj.jsonEnabled = message.jsonEnabled;
    }
    if (message.tlsEnabled !== false) {
      obj.tlsEnabled = message.tlsEnabled;
    }
    if (message.root !== "") {
      obj.root = message.root;
    }
    if (message.proxyToClientEnabled !== false) {
      obj.proxyToClientEnabled = message.proxyToClientEnabled;
    }
    if (message.mapReportingEnabled !== false) {
      obj.mapReportingEnabled = message.mapReportingEnabled;
    }
    if (message.mapReportSettings !== undefined) {
      obj.mapReportSettings = ModuleConfig_MapReportSettings.toJSON(message.mapReportSettings);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_MQTTConfig>, I>>(base?: I): ModuleConfig_MQTTConfig {
    return ModuleConfig_MQTTConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_MQTTConfig>, I>>(object: I): ModuleConfig_MQTTConfig {
    const message = createBaseModuleConfig_MQTTConfig();
    message.enabled = object.enabled ?? false;
    message.address = object.address ?? "";
    message.username = object.username ?? "";
    message.password = object.password ?? "";
    message.encryptionEnabled = object.encryptionEnabled ?? false;
    message.jsonEnabled = object.jsonEnabled ?? false;
    message.tlsEnabled = object.tlsEnabled ?? false;
    message.root = object.root ?? "";
    message.proxyToClientEnabled = object.proxyToClientEnabled ?? false;
    message.mapReportingEnabled = object.mapReportingEnabled ?? false;
    message.mapReportSettings = (object.mapReportSettings !== undefined && object.mapReportSettings !== null)
      ? ModuleConfig_MapReportSettings.fromPartial(object.mapReportSettings)
      : undefined;
    return message;
  },
};

function createBaseModuleConfig_MapReportSettings(): ModuleConfig_MapReportSettings {
  return { publishIntervalSecs: 0, positionPrecision: 0, shouldReportLocation: false };
}

export const ModuleConfig_MapReportSettings = {
  encode(message: ModuleConfig_MapReportSettings, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.publishIntervalSecs !== 0) {
      writer.uint32(8).uint32(message.publishIntervalSecs);
    }
    if (message.positionPrecision !== 0) {
      writer.uint32(16).uint32(message.positionPrecision);
    }
    if (message.shouldReportLocation !== false) {
      writer.uint32(24).bool(message.shouldReportLocation);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_MapReportSettings {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_MapReportSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.publishIntervalSecs = reader.uint32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.positionPrecision = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.shouldReportLocation = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_MapReportSettings {
    return {
      publishIntervalSecs: isSet(object.publishIntervalSecs) ? globalThis.Number(object.publishIntervalSecs) : 0,
      positionPrecision: isSet(object.positionPrecision) ? globalThis.Number(object.positionPrecision) : 0,
      shouldReportLocation: isSet(object.shouldReportLocation)
        ? globalThis.Boolean(object.shouldReportLocation)
        : false,
    };
  },

  toJSON(message: ModuleConfig_MapReportSettings): unknown {
    const obj: any = {};
    if (message.publishIntervalSecs !== 0) {
      obj.publishIntervalSecs = Math.round(message.publishIntervalSecs);
    }
    if (message.positionPrecision !== 0) {
      obj.positionPrecision = Math.round(message.positionPrecision);
    }
    if (message.shouldReportLocation !== false) {
      obj.shouldReportLocation = message.shouldReportLocation;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_MapReportSettings>, I>>(base?: I): ModuleConfig_MapReportSettings {
    return ModuleConfig_MapReportSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_MapReportSettings>, I>>(
    object: I,
  ): ModuleConfig_MapReportSettings {
    const message = createBaseModuleConfig_MapReportSettings();
    message.publishIntervalSecs = object.publishIntervalSecs ?? 0;
    message.positionPrecision = object.positionPrecision ?? 0;
    message.shouldReportLocation = object.shouldReportLocation ?? false;
    return message;
  },
};

function createBaseModuleConfig_RemoteHardwareConfig(): ModuleConfig_RemoteHardwareConfig {
  return { enabled: false, allowUndefinedPinAccess: false, availablePins: [] };
}

export const ModuleConfig_RemoteHardwareConfig = {
  encode(message: ModuleConfig_RemoteHardwareConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.allowUndefinedPinAccess !== false) {
      writer.uint32(16).bool(message.allowUndefinedPinAccess);
    }
    for (const v of message.availablePins) {
      RemoteHardwarePin.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_RemoteHardwareConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_RemoteHardwareConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.allowUndefinedPinAccess = reader.bool();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.availablePins.push(RemoteHardwarePin.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_RemoteHardwareConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      allowUndefinedPinAccess: isSet(object.allowUndefinedPinAccess)
        ? globalThis.Boolean(object.allowUndefinedPinAccess)
        : false,
      availablePins: globalThis.Array.isArray(object?.availablePins)
        ? object.availablePins.map((e: any) => RemoteHardwarePin.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ModuleConfig_RemoteHardwareConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.allowUndefinedPinAccess !== false) {
      obj.allowUndefinedPinAccess = message.allowUndefinedPinAccess;
    }
    if (message.availablePins?.length) {
      obj.availablePins = message.availablePins.map((e) => RemoteHardwarePin.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_RemoteHardwareConfig>, I>>(
    base?: I,
  ): ModuleConfig_RemoteHardwareConfig {
    return ModuleConfig_RemoteHardwareConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_RemoteHardwareConfig>, I>>(
    object: I,
  ): ModuleConfig_RemoteHardwareConfig {
    const message = createBaseModuleConfig_RemoteHardwareConfig();
    message.enabled = object.enabled ?? false;
    message.allowUndefinedPinAccess = object.allowUndefinedPinAccess ?? false;
    message.availablePins = object.availablePins?.map((e) => RemoteHardwarePin.fromPartial(e)) || [];
    return message;
  },
};

function createBaseModuleConfig_NeighborInfoConfig(): ModuleConfig_NeighborInfoConfig {
  return { enabled: false, updateInterval: 0, transmitOverLora: false };
}

export const ModuleConfig_NeighborInfoConfig = {
  encode(message: ModuleConfig_NeighborInfoConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.updateInterval !== 0) {
      writer.uint32(16).uint32(message.updateInterval);
    }
    if (message.transmitOverLora !== false) {
      writer.uint32(24).bool(message.transmitOverLora);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_NeighborInfoConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_NeighborInfoConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.updateInterval = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.transmitOverLora = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_NeighborInfoConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      updateInterval: isSet(object.updateInterval) ? globalThis.Number(object.updateInterval) : 0,
      transmitOverLora: isSet(object.transmitOverLora) ? globalThis.Boolean(object.transmitOverLora) : false,
    };
  },

  toJSON(message: ModuleConfig_NeighborInfoConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.updateInterval !== 0) {
      obj.updateInterval = Math.round(message.updateInterval);
    }
    if (message.transmitOverLora !== false) {
      obj.transmitOverLora = message.transmitOverLora;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_NeighborInfoConfig>, I>>(base?: I): ModuleConfig_NeighborInfoConfig {
    return ModuleConfig_NeighborInfoConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_NeighborInfoConfig>, I>>(
    object: I,
  ): ModuleConfig_NeighborInfoConfig {
    const message = createBaseModuleConfig_NeighborInfoConfig();
    message.enabled = object.enabled ?? false;
    message.updateInterval = object.updateInterval ?? 0;
    message.transmitOverLora = object.transmitOverLora ?? false;
    return message;
  },
};

function createBaseModuleConfig_DetectionSensorConfig(): ModuleConfig_DetectionSensorConfig {
  return {
    enabled: false,
    minimumBroadcastSecs: 0,
    stateBroadcastSecs: 0,
    sendBell: false,
    name: "",
    monitorPin: 0,
    detectionTriggerType: 0,
    usePullup: false,
  };
}

export const ModuleConfig_DetectionSensorConfig = {
  encode(message: ModuleConfig_DetectionSensorConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.minimumBroadcastSecs !== 0) {
      writer.uint32(16).uint32(message.minimumBroadcastSecs);
    }
    if (message.stateBroadcastSecs !== 0) {
      writer.uint32(24).uint32(message.stateBroadcastSecs);
    }
    if (message.sendBell !== false) {
      writer.uint32(32).bool(message.sendBell);
    }
    if (message.name !== "") {
      writer.uint32(42).string(message.name);
    }
    if (message.monitorPin !== 0) {
      writer.uint32(48).uint32(message.monitorPin);
    }
    if (message.detectionTriggerType !== 0) {
      writer.uint32(56).int32(message.detectionTriggerType);
    }
    if (message.usePullup !== false) {
      writer.uint32(64).bool(message.usePullup);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_DetectionSensorConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_DetectionSensorConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.minimumBroadcastSecs = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.stateBroadcastSecs = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.sendBell = reader.bool();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.name = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.monitorPin = reader.uint32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.detectionTriggerType = reader.int32() as any;
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.usePullup = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_DetectionSensorConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      minimumBroadcastSecs: isSet(object.minimumBroadcastSecs) ? globalThis.Number(object.minimumBroadcastSecs) : 0,
      stateBroadcastSecs: isSet(object.stateBroadcastSecs) ? globalThis.Number(object.stateBroadcastSecs) : 0,
      sendBell: isSet(object.sendBell) ? globalThis.Boolean(object.sendBell) : false,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      monitorPin: isSet(object.monitorPin) ? globalThis.Number(object.monitorPin) : 0,
      detectionTriggerType: isSet(object.detectionTriggerType)
        ? moduleConfig_DetectionSensorConfig_TriggerTypeFromJSON(object.detectionTriggerType)
        : 0,
      usePullup: isSet(object.usePullup) ? globalThis.Boolean(object.usePullup) : false,
    };
  },

  toJSON(message: ModuleConfig_DetectionSensorConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.minimumBroadcastSecs !== 0) {
      obj.minimumBroadcastSecs = Math.round(message.minimumBroadcastSecs);
    }
    if (message.stateBroadcastSecs !== 0) {
      obj.stateBroadcastSecs = Math.round(message.stateBroadcastSecs);
    }
    if (message.sendBell !== false) {
      obj.sendBell = message.sendBell;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.monitorPin !== 0) {
      obj.monitorPin = Math.round(message.monitorPin);
    }
    if (message.detectionTriggerType !== 0) {
      obj.detectionTriggerType = moduleConfig_DetectionSensorConfig_TriggerTypeToJSON(message.detectionTriggerType);
    }
    if (message.usePullup !== false) {
      obj.usePullup = message.usePullup;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_DetectionSensorConfig>, I>>(
    base?: I,
  ): ModuleConfig_DetectionSensorConfig {
    return ModuleConfig_DetectionSensorConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_DetectionSensorConfig>, I>>(
    object: I,
  ): ModuleConfig_DetectionSensorConfig {
    const message = createBaseModuleConfig_DetectionSensorConfig();
    message.enabled = object.enabled ?? false;
    message.minimumBroadcastSecs = object.minimumBroadcastSecs ?? 0;
    message.stateBroadcastSecs = object.stateBroadcastSecs ?? 0;
    message.sendBell = object.sendBell ?? false;
    message.name = object.name ?? "";
    message.monitorPin = object.monitorPin ?? 0;
    message.detectionTriggerType = object.detectionTriggerType ?? 0;
    message.usePullup = object.usePullup ?? false;
    return message;
  },
};

function createBaseModuleConfig_AudioConfig(): ModuleConfig_AudioConfig {
  return { codec2Enabled: false, pttPin: 0, bitrate: 0, i2sWs: 0, i2sSd: 0, i2sDin: 0, i2sSck: 0 };
}

export const ModuleConfig_AudioConfig = {
  encode(message: ModuleConfig_AudioConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.codec2Enabled !== false) {
      writer.uint32(8).bool(message.codec2Enabled);
    }
    if (message.pttPin !== 0) {
      writer.uint32(16).uint32(message.pttPin);
    }
    if (message.bitrate !== 0) {
      writer.uint32(24).int32(message.bitrate);
    }
    if (message.i2sWs !== 0) {
      writer.uint32(32).uint32(message.i2sWs);
    }
    if (message.i2sSd !== 0) {
      writer.uint32(40).uint32(message.i2sSd);
    }
    if (message.i2sDin !== 0) {
      writer.uint32(48).uint32(message.i2sDin);
    }
    if (message.i2sSck !== 0) {
      writer.uint32(56).uint32(message.i2sSck);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_AudioConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_AudioConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.codec2Enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.pttPin = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.bitrate = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.i2sWs = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.i2sSd = reader.uint32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.i2sDin = reader.uint32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.i2sSck = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_AudioConfig {
    return {
      codec2Enabled: isSet(object.codec2Enabled) ? globalThis.Boolean(object.codec2Enabled) : false,
      pttPin: isSet(object.pttPin) ? globalThis.Number(object.pttPin) : 0,
      bitrate: isSet(object.bitrate) ? moduleConfig_AudioConfig_AudioBaudFromJSON(object.bitrate) : 0,
      i2sWs: isSet(object.i2sWs) ? globalThis.Number(object.i2sWs) : 0,
      i2sSd: isSet(object.i2sSd) ? globalThis.Number(object.i2sSd) : 0,
      i2sDin: isSet(object.i2sDin) ? globalThis.Number(object.i2sDin) : 0,
      i2sSck: isSet(object.i2sSck) ? globalThis.Number(object.i2sSck) : 0,
    };
  },

  toJSON(message: ModuleConfig_AudioConfig): unknown {
    const obj: any = {};
    if (message.codec2Enabled !== false) {
      obj.codec2Enabled = message.codec2Enabled;
    }
    if (message.pttPin !== 0) {
      obj.pttPin = Math.round(message.pttPin);
    }
    if (message.bitrate !== 0) {
      obj.bitrate = moduleConfig_AudioConfig_AudioBaudToJSON(message.bitrate);
    }
    if (message.i2sWs !== 0) {
      obj.i2sWs = Math.round(message.i2sWs);
    }
    if (message.i2sSd !== 0) {
      obj.i2sSd = Math.round(message.i2sSd);
    }
    if (message.i2sDin !== 0) {
      obj.i2sDin = Math.round(message.i2sDin);
    }
    if (message.i2sSck !== 0) {
      obj.i2sSck = Math.round(message.i2sSck);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_AudioConfig>, I>>(base?: I): ModuleConfig_AudioConfig {
    return ModuleConfig_AudioConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_AudioConfig>, I>>(object: I): ModuleConfig_AudioConfig {
    const message = createBaseModuleConfig_AudioConfig();
    message.codec2Enabled = object.codec2Enabled ?? false;
    message.pttPin = object.pttPin ?? 0;
    message.bitrate = object.bitrate ?? 0;
    message.i2sWs = object.i2sWs ?? 0;
    message.i2sSd = object.i2sSd ?? 0;
    message.i2sDin = object.i2sDin ?? 0;
    message.i2sSck = object.i2sSck ?? 0;
    return message;
  },
};

function createBaseModuleConfig_PaxcounterConfig(): ModuleConfig_PaxcounterConfig {
  return { enabled: false, paxcounterUpdateInterval: 0, wifiThreshold: 0, bleThreshold: 0 };
}

export const ModuleConfig_PaxcounterConfig = {
  encode(message: ModuleConfig_PaxcounterConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.paxcounterUpdateInterval !== 0) {
      writer.uint32(16).uint32(message.paxcounterUpdateInterval);
    }
    if (message.wifiThreshold !== 0) {
      writer.uint32(24).int32(message.wifiThreshold);
    }
    if (message.bleThreshold !== 0) {
      writer.uint32(32).int32(message.bleThreshold);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_PaxcounterConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_PaxcounterConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.paxcounterUpdateInterval = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.wifiThreshold = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.bleThreshold = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_PaxcounterConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      paxcounterUpdateInterval: isSet(object.paxcounterUpdateInterval)
        ? globalThis.Number(object.paxcounterUpdateInterval)
        : 0,
      wifiThreshold: isSet(object.wifiThreshold) ? globalThis.Number(object.wifiThreshold) : 0,
      bleThreshold: isSet(object.bleThreshold) ? globalThis.Number(object.bleThreshold) : 0,
    };
  },

  toJSON(message: ModuleConfig_PaxcounterConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.paxcounterUpdateInterval !== 0) {
      obj.paxcounterUpdateInterval = Math.round(message.paxcounterUpdateInterval);
    }
    if (message.wifiThreshold !== 0) {
      obj.wifiThreshold = Math.round(message.wifiThreshold);
    }
    if (message.bleThreshold !== 0) {
      obj.bleThreshold = Math.round(message.bleThreshold);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_PaxcounterConfig>, I>>(base?: I): ModuleConfig_PaxcounterConfig {
    return ModuleConfig_PaxcounterConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_PaxcounterConfig>, I>>(
    object: I,
  ): ModuleConfig_PaxcounterConfig {
    const message = createBaseModuleConfig_PaxcounterConfig();
    message.enabled = object.enabled ?? false;
    message.paxcounterUpdateInterval = object.paxcounterUpdateInterval ?? 0;
    message.wifiThreshold = object.wifiThreshold ?? 0;
    message.bleThreshold = object.bleThreshold ?? 0;
    return message;
  },
};

function createBaseModuleConfig_TrafficManagementConfig(): ModuleConfig_TrafficManagementConfig {
  return {
    positionMinIntervalSecs: 0,
    nodeinfoDirectResponseMaxHops: 0,
    rateLimitWindowSecs: 0,
    rateLimitMaxPackets: 0,
    unknownPacketThreshold: 0,
  };
}

export const ModuleConfig_TrafficManagementConfig = {
  encode(message: ModuleConfig_TrafficManagementConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.positionMinIntervalSecs !== 0) {
      writer.uint32(32).uint32(message.positionMinIntervalSecs);
    }
    if (message.nodeinfoDirectResponseMaxHops !== 0) {
      writer.uint32(48).uint32(message.nodeinfoDirectResponseMaxHops);
    }
    if (message.rateLimitWindowSecs !== 0) {
      writer.uint32(64).uint32(message.rateLimitWindowSecs);
    }
    if (message.rateLimitMaxPackets !== 0) {
      writer.uint32(72).uint32(message.rateLimitMaxPackets);
    }
    if (message.unknownPacketThreshold !== 0) {
      writer.uint32(88).uint32(message.unknownPacketThreshold);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_TrafficManagementConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_TrafficManagementConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 4:
          if (tag !== 32) {
            break;
          }

          message.positionMinIntervalSecs = reader.uint32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.nodeinfoDirectResponseMaxHops = reader.uint32();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.rateLimitWindowSecs = reader.uint32();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.rateLimitMaxPackets = reader.uint32();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.unknownPacketThreshold = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_TrafficManagementConfig {
    return {
      positionMinIntervalSecs: isSet(object.positionMinIntervalSecs)
        ? globalThis.Number(object.positionMinIntervalSecs)
        : 0,
      nodeinfoDirectResponseMaxHops: isSet(object.nodeinfoDirectResponseMaxHops)
        ? globalThis.Number(object.nodeinfoDirectResponseMaxHops)
        : 0,
      rateLimitWindowSecs: isSet(object.rateLimitWindowSecs) ? globalThis.Number(object.rateLimitWindowSecs) : 0,
      rateLimitMaxPackets: isSet(object.rateLimitMaxPackets) ? globalThis.Number(object.rateLimitMaxPackets) : 0,
      unknownPacketThreshold: isSet(object.unknownPacketThreshold)
        ? globalThis.Number(object.unknownPacketThreshold)
        : 0,
    };
  },

  toJSON(message: ModuleConfig_TrafficManagementConfig): unknown {
    const obj: any = {};
    if (message.positionMinIntervalSecs !== 0) {
      obj.positionMinIntervalSecs = Math.round(message.positionMinIntervalSecs);
    }
    if (message.nodeinfoDirectResponseMaxHops !== 0) {
      obj.nodeinfoDirectResponseMaxHops = Math.round(message.nodeinfoDirectResponseMaxHops);
    }
    if (message.rateLimitWindowSecs !== 0) {
      obj.rateLimitWindowSecs = Math.round(message.rateLimitWindowSecs);
    }
    if (message.rateLimitMaxPackets !== 0) {
      obj.rateLimitMaxPackets = Math.round(message.rateLimitMaxPackets);
    }
    if (message.unknownPacketThreshold !== 0) {
      obj.unknownPacketThreshold = Math.round(message.unknownPacketThreshold);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_TrafficManagementConfig>, I>>(
    base?: I,
  ): ModuleConfig_TrafficManagementConfig {
    return ModuleConfig_TrafficManagementConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_TrafficManagementConfig>, I>>(
    object: I,
  ): ModuleConfig_TrafficManagementConfig {
    const message = createBaseModuleConfig_TrafficManagementConfig();
    message.positionMinIntervalSecs = object.positionMinIntervalSecs ?? 0;
    message.nodeinfoDirectResponseMaxHops = object.nodeinfoDirectResponseMaxHops ?? 0;
    message.rateLimitWindowSecs = object.rateLimitWindowSecs ?? 0;
    message.rateLimitMaxPackets = object.rateLimitMaxPackets ?? 0;
    message.unknownPacketThreshold = object.unknownPacketThreshold ?? 0;
    return message;
  },
};

function createBaseModuleConfig_SerialConfig(): ModuleConfig_SerialConfig {
  return {
    enabled: false,
    echo: false,
    rxd: 0,
    txd: 0,
    baud: 0,
    timeout: 0,
    mode: 0,
    overrideConsoleSerialPort: false,
  };
}

export const ModuleConfig_SerialConfig = {
  encode(message: ModuleConfig_SerialConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.echo !== false) {
      writer.uint32(16).bool(message.echo);
    }
    if (message.rxd !== 0) {
      writer.uint32(24).uint32(message.rxd);
    }
    if (message.txd !== 0) {
      writer.uint32(32).uint32(message.txd);
    }
    if (message.baud !== 0) {
      writer.uint32(40).int32(message.baud);
    }
    if (message.timeout !== 0) {
      writer.uint32(48).uint32(message.timeout);
    }
    if (message.mode !== 0) {
      writer.uint32(56).int32(message.mode);
    }
    if (message.overrideConsoleSerialPort !== false) {
      writer.uint32(64).bool(message.overrideConsoleSerialPort);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_SerialConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_SerialConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.echo = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.rxd = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.txd = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.baud = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.timeout = reader.uint32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.overrideConsoleSerialPort = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_SerialConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      echo: isSet(object.echo) ? globalThis.Boolean(object.echo) : false,
      rxd: isSet(object.rxd) ? globalThis.Number(object.rxd) : 0,
      txd: isSet(object.txd) ? globalThis.Number(object.txd) : 0,
      baud: isSet(object.baud) ? moduleConfig_SerialConfig_SerialBaudFromJSON(object.baud) : 0,
      timeout: isSet(object.timeout) ? globalThis.Number(object.timeout) : 0,
      mode: isSet(object.mode) ? moduleConfig_SerialConfig_SerialModeFromJSON(object.mode) : 0,
      overrideConsoleSerialPort: isSet(object.overrideConsoleSerialPort)
        ? globalThis.Boolean(object.overrideConsoleSerialPort)
        : false,
    };
  },

  toJSON(message: ModuleConfig_SerialConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.echo !== false) {
      obj.echo = message.echo;
    }
    if (message.rxd !== 0) {
      obj.rxd = Math.round(message.rxd);
    }
    if (message.txd !== 0) {
      obj.txd = Math.round(message.txd);
    }
    if (message.baud !== 0) {
      obj.baud = moduleConfig_SerialConfig_SerialBaudToJSON(message.baud);
    }
    if (message.timeout !== 0) {
      obj.timeout = Math.round(message.timeout);
    }
    if (message.mode !== 0) {
      obj.mode = moduleConfig_SerialConfig_SerialModeToJSON(message.mode);
    }
    if (message.overrideConsoleSerialPort !== false) {
      obj.overrideConsoleSerialPort = message.overrideConsoleSerialPort;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_SerialConfig>, I>>(base?: I): ModuleConfig_SerialConfig {
    return ModuleConfig_SerialConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_SerialConfig>, I>>(object: I): ModuleConfig_SerialConfig {
    const message = createBaseModuleConfig_SerialConfig();
    message.enabled = object.enabled ?? false;
    message.echo = object.echo ?? false;
    message.rxd = object.rxd ?? 0;
    message.txd = object.txd ?? 0;
    message.baud = object.baud ?? 0;
    message.timeout = object.timeout ?? 0;
    message.mode = object.mode ?? 0;
    message.overrideConsoleSerialPort = object.overrideConsoleSerialPort ?? false;
    return message;
  },
};

function createBaseModuleConfig_ExternalNotificationConfig(): ModuleConfig_ExternalNotificationConfig {
  return {
    enabled: false,
    outputMs: 0,
    output: 0,
    outputVibra: 0,
    outputBuzzer: 0,
    active: false,
    alertMessage: false,
    alertMessageVibra: false,
    alertMessageBuzzer: false,
    alertBell: false,
    alertBellVibra: false,
    alertBellBuzzer: false,
    usePwm: false,
    nagTimeout: 0,
    useI2sAsBuzzer: false,
  };
}

export const ModuleConfig_ExternalNotificationConfig = {
  encode(message: ModuleConfig_ExternalNotificationConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.outputMs !== 0) {
      writer.uint32(16).uint32(message.outputMs);
    }
    if (message.output !== 0) {
      writer.uint32(24).uint32(message.output);
    }
    if (message.outputVibra !== 0) {
      writer.uint32(64).uint32(message.outputVibra);
    }
    if (message.outputBuzzer !== 0) {
      writer.uint32(72).uint32(message.outputBuzzer);
    }
    if (message.active !== false) {
      writer.uint32(32).bool(message.active);
    }
    if (message.alertMessage !== false) {
      writer.uint32(40).bool(message.alertMessage);
    }
    if (message.alertMessageVibra !== false) {
      writer.uint32(80).bool(message.alertMessageVibra);
    }
    if (message.alertMessageBuzzer !== false) {
      writer.uint32(88).bool(message.alertMessageBuzzer);
    }
    if (message.alertBell !== false) {
      writer.uint32(48).bool(message.alertBell);
    }
    if (message.alertBellVibra !== false) {
      writer.uint32(96).bool(message.alertBellVibra);
    }
    if (message.alertBellBuzzer !== false) {
      writer.uint32(104).bool(message.alertBellBuzzer);
    }
    if (message.usePwm !== false) {
      writer.uint32(56).bool(message.usePwm);
    }
    if (message.nagTimeout !== 0) {
      writer.uint32(112).uint32(message.nagTimeout);
    }
    if (message.useI2sAsBuzzer !== false) {
      writer.uint32(120).bool(message.useI2sAsBuzzer);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_ExternalNotificationConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_ExternalNotificationConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.outputMs = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.output = reader.uint32();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.outputVibra = reader.uint32();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.outputBuzzer = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.active = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.alertMessage = reader.bool();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.alertMessageVibra = reader.bool();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.alertMessageBuzzer = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.alertBell = reader.bool();
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.alertBellVibra = reader.bool();
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.alertBellBuzzer = reader.bool();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.usePwm = reader.bool();
          continue;
        case 14:
          if (tag !== 112) {
            break;
          }

          message.nagTimeout = reader.uint32();
          continue;
        case 15:
          if (tag !== 120) {
            break;
          }

          message.useI2sAsBuzzer = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_ExternalNotificationConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      outputMs: isSet(object.outputMs) ? globalThis.Number(object.outputMs) : 0,
      output: isSet(object.output) ? globalThis.Number(object.output) : 0,
      outputVibra: isSet(object.outputVibra) ? globalThis.Number(object.outputVibra) : 0,
      outputBuzzer: isSet(object.outputBuzzer) ? globalThis.Number(object.outputBuzzer) : 0,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
      alertMessage: isSet(object.alertMessage) ? globalThis.Boolean(object.alertMessage) : false,
      alertMessageVibra: isSet(object.alertMessageVibra) ? globalThis.Boolean(object.alertMessageVibra) : false,
      alertMessageBuzzer: isSet(object.alertMessageBuzzer) ? globalThis.Boolean(object.alertMessageBuzzer) : false,
      alertBell: isSet(object.alertBell) ? globalThis.Boolean(object.alertBell) : false,
      alertBellVibra: isSet(object.alertBellVibra) ? globalThis.Boolean(object.alertBellVibra) : false,
      alertBellBuzzer: isSet(object.alertBellBuzzer) ? globalThis.Boolean(object.alertBellBuzzer) : false,
      usePwm: isSet(object.usePwm) ? globalThis.Boolean(object.usePwm) : false,
      nagTimeout: isSet(object.nagTimeout) ? globalThis.Number(object.nagTimeout) : 0,
      useI2sAsBuzzer: isSet(object.useI2sAsBuzzer) ? globalThis.Boolean(object.useI2sAsBuzzer) : false,
    };
  },

  toJSON(message: ModuleConfig_ExternalNotificationConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.outputMs !== 0) {
      obj.outputMs = Math.round(message.outputMs);
    }
    if (message.output !== 0) {
      obj.output = Math.round(message.output);
    }
    if (message.outputVibra !== 0) {
      obj.outputVibra = Math.round(message.outputVibra);
    }
    if (message.outputBuzzer !== 0) {
      obj.outputBuzzer = Math.round(message.outputBuzzer);
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    if (message.alertMessage !== false) {
      obj.alertMessage = message.alertMessage;
    }
    if (message.alertMessageVibra !== false) {
      obj.alertMessageVibra = message.alertMessageVibra;
    }
    if (message.alertMessageBuzzer !== false) {
      obj.alertMessageBuzzer = message.alertMessageBuzzer;
    }
    if (message.alertBell !== false) {
      obj.alertBell = message.alertBell;
    }
    if (message.alertBellVibra !== false) {
      obj.alertBellVibra = message.alertBellVibra;
    }
    if (message.alertBellBuzzer !== false) {
      obj.alertBellBuzzer = message.alertBellBuzzer;
    }
    if (message.usePwm !== false) {
      obj.usePwm = message.usePwm;
    }
    if (message.nagTimeout !== 0) {
      obj.nagTimeout = Math.round(message.nagTimeout);
    }
    if (message.useI2sAsBuzzer !== false) {
      obj.useI2sAsBuzzer = message.useI2sAsBuzzer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_ExternalNotificationConfig>, I>>(
    base?: I,
  ): ModuleConfig_ExternalNotificationConfig {
    return ModuleConfig_ExternalNotificationConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_ExternalNotificationConfig>, I>>(
    object: I,
  ): ModuleConfig_ExternalNotificationConfig {
    const message = createBaseModuleConfig_ExternalNotificationConfig();
    message.enabled = object.enabled ?? false;
    message.outputMs = object.outputMs ?? 0;
    message.output = object.output ?? 0;
    message.outputVibra = object.outputVibra ?? 0;
    message.outputBuzzer = object.outputBuzzer ?? 0;
    message.active = object.active ?? false;
    message.alertMessage = object.alertMessage ?? false;
    message.alertMessageVibra = object.alertMessageVibra ?? false;
    message.alertMessageBuzzer = object.alertMessageBuzzer ?? false;
    message.alertBell = object.alertBell ?? false;
    message.alertBellVibra = object.alertBellVibra ?? false;
    message.alertBellBuzzer = object.alertBellBuzzer ?? false;
    message.usePwm = object.usePwm ?? false;
    message.nagTimeout = object.nagTimeout ?? 0;
    message.useI2sAsBuzzer = object.useI2sAsBuzzer ?? false;
    return message;
  },
};

function createBaseModuleConfig_StoreForwardConfig(): ModuleConfig_StoreForwardConfig {
  return { enabled: false, heartbeat: false, records: 0, historyReturnMax: 0, historyReturnWindow: 0, isServer: false };
}

export const ModuleConfig_StoreForwardConfig = {
  encode(message: ModuleConfig_StoreForwardConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.heartbeat !== false) {
      writer.uint32(16).bool(message.heartbeat);
    }
    if (message.records !== 0) {
      writer.uint32(24).uint32(message.records);
    }
    if (message.historyReturnMax !== 0) {
      writer.uint32(32).uint32(message.historyReturnMax);
    }
    if (message.historyReturnWindow !== 0) {
      writer.uint32(40).uint32(message.historyReturnWindow);
    }
    if (message.isServer !== false) {
      writer.uint32(48).bool(message.isServer);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_StoreForwardConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_StoreForwardConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.heartbeat = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.records = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.historyReturnMax = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.historyReturnWindow = reader.uint32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.isServer = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_StoreForwardConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      heartbeat: isSet(object.heartbeat) ? globalThis.Boolean(object.heartbeat) : false,
      records: isSet(object.records) ? globalThis.Number(object.records) : 0,
      historyReturnMax: isSet(object.historyReturnMax) ? globalThis.Number(object.historyReturnMax) : 0,
      historyReturnWindow: isSet(object.historyReturnWindow) ? globalThis.Number(object.historyReturnWindow) : 0,
      isServer: isSet(object.isServer) ? globalThis.Boolean(object.isServer) : false,
    };
  },

  toJSON(message: ModuleConfig_StoreForwardConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.heartbeat !== false) {
      obj.heartbeat = message.heartbeat;
    }
    if (message.records !== 0) {
      obj.records = Math.round(message.records);
    }
    if (message.historyReturnMax !== 0) {
      obj.historyReturnMax = Math.round(message.historyReturnMax);
    }
    if (message.historyReturnWindow !== 0) {
      obj.historyReturnWindow = Math.round(message.historyReturnWindow);
    }
    if (message.isServer !== false) {
      obj.isServer = message.isServer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_StoreForwardConfig>, I>>(base?: I): ModuleConfig_StoreForwardConfig {
    return ModuleConfig_StoreForwardConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_StoreForwardConfig>, I>>(
    object: I,
  ): ModuleConfig_StoreForwardConfig {
    const message = createBaseModuleConfig_StoreForwardConfig();
    message.enabled = object.enabled ?? false;
    message.heartbeat = object.heartbeat ?? false;
    message.records = object.records ?? 0;
    message.historyReturnMax = object.historyReturnMax ?? 0;
    message.historyReturnWindow = object.historyReturnWindow ?? 0;
    message.isServer = object.isServer ?? false;
    return message;
  },
};

function createBaseModuleConfig_RangeTestConfig(): ModuleConfig_RangeTestConfig {
  return { enabled: false, sender: 0, save: false, clearOnReboot: false };
}

export const ModuleConfig_RangeTestConfig = {
  encode(message: ModuleConfig_RangeTestConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.sender !== 0) {
      writer.uint32(16).uint32(message.sender);
    }
    if (message.save !== false) {
      writer.uint32(24).bool(message.save);
    }
    if (message.clearOnReboot !== false) {
      writer.uint32(32).bool(message.clearOnReboot);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_RangeTestConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_RangeTestConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.sender = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.save = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.clearOnReboot = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_RangeTestConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      sender: isSet(object.sender) ? globalThis.Number(object.sender) : 0,
      save: isSet(object.save) ? globalThis.Boolean(object.save) : false,
      clearOnReboot: isSet(object.clearOnReboot) ? globalThis.Boolean(object.clearOnReboot) : false,
    };
  },

  toJSON(message: ModuleConfig_RangeTestConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.sender !== 0) {
      obj.sender = Math.round(message.sender);
    }
    if (message.save !== false) {
      obj.save = message.save;
    }
    if (message.clearOnReboot !== false) {
      obj.clearOnReboot = message.clearOnReboot;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_RangeTestConfig>, I>>(base?: I): ModuleConfig_RangeTestConfig {
    return ModuleConfig_RangeTestConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_RangeTestConfig>, I>>(object: I): ModuleConfig_RangeTestConfig {
    const message = createBaseModuleConfig_RangeTestConfig();
    message.enabled = object.enabled ?? false;
    message.sender = object.sender ?? 0;
    message.save = object.save ?? false;
    message.clearOnReboot = object.clearOnReboot ?? false;
    return message;
  },
};

function createBaseModuleConfig_TelemetryConfig(): ModuleConfig_TelemetryConfig {
  return {
    deviceUpdateInterval: 0,
    environmentUpdateInterval: 0,
    environmentMeasurementEnabled: false,
    environmentScreenEnabled: false,
    environmentDisplayFahrenheit: false,
    airQualityEnabled: false,
    airQualityInterval: 0,
    powerMeasurementEnabled: false,
    powerUpdateInterval: 0,
    powerScreenEnabled: false,
    healthMeasurementEnabled: false,
    healthUpdateInterval: 0,
    healthScreenEnabled: false,
    deviceTelemetryEnabled: false,
    airQualityScreenEnabled: false,
  };
}

export const ModuleConfig_TelemetryConfig = {
  encode(message: ModuleConfig_TelemetryConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.deviceUpdateInterval !== 0) {
      writer.uint32(8).uint32(message.deviceUpdateInterval);
    }
    if (message.environmentUpdateInterval !== 0) {
      writer.uint32(16).uint32(message.environmentUpdateInterval);
    }
    if (message.environmentMeasurementEnabled !== false) {
      writer.uint32(24).bool(message.environmentMeasurementEnabled);
    }
    if (message.environmentScreenEnabled !== false) {
      writer.uint32(32).bool(message.environmentScreenEnabled);
    }
    if (message.environmentDisplayFahrenheit !== false) {
      writer.uint32(40).bool(message.environmentDisplayFahrenheit);
    }
    if (message.airQualityEnabled !== false) {
      writer.uint32(48).bool(message.airQualityEnabled);
    }
    if (message.airQualityInterval !== 0) {
      writer.uint32(56).uint32(message.airQualityInterval);
    }
    if (message.powerMeasurementEnabled !== false) {
      writer.uint32(64).bool(message.powerMeasurementEnabled);
    }
    if (message.powerUpdateInterval !== 0) {
      writer.uint32(72).uint32(message.powerUpdateInterval);
    }
    if (message.powerScreenEnabled !== false) {
      writer.uint32(80).bool(message.powerScreenEnabled);
    }
    if (message.healthMeasurementEnabled !== false) {
      writer.uint32(88).bool(message.healthMeasurementEnabled);
    }
    if (message.healthUpdateInterval !== 0) {
      writer.uint32(96).uint32(message.healthUpdateInterval);
    }
    if (message.healthScreenEnabled !== false) {
      writer.uint32(104).bool(message.healthScreenEnabled);
    }
    if (message.deviceTelemetryEnabled !== false) {
      writer.uint32(112).bool(message.deviceTelemetryEnabled);
    }
    if (message.airQualityScreenEnabled !== false) {
      writer.uint32(120).bool(message.airQualityScreenEnabled);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_TelemetryConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_TelemetryConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.deviceUpdateInterval = reader.uint32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.environmentUpdateInterval = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.environmentMeasurementEnabled = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.environmentScreenEnabled = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.environmentDisplayFahrenheit = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.airQualityEnabled = reader.bool();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.airQualityInterval = reader.uint32();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.powerMeasurementEnabled = reader.bool();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.powerUpdateInterval = reader.uint32();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.powerScreenEnabled = reader.bool();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.healthMeasurementEnabled = reader.bool();
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.healthUpdateInterval = reader.uint32();
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.healthScreenEnabled = reader.bool();
          continue;
        case 14:
          if (tag !== 112) {
            break;
          }

          message.deviceTelemetryEnabled = reader.bool();
          continue;
        case 15:
          if (tag !== 120) {
            break;
          }

          message.airQualityScreenEnabled = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_TelemetryConfig {
    return {
      deviceUpdateInterval: isSet(object.deviceUpdateInterval) ? globalThis.Number(object.deviceUpdateInterval) : 0,
      environmentUpdateInterval: isSet(object.environmentUpdateInterval)
        ? globalThis.Number(object.environmentUpdateInterval)
        : 0,
      environmentMeasurementEnabled: isSet(object.environmentMeasurementEnabled)
        ? globalThis.Boolean(object.environmentMeasurementEnabled)
        : false,
      environmentScreenEnabled: isSet(object.environmentScreenEnabled)
        ? globalThis.Boolean(object.environmentScreenEnabled)
        : false,
      environmentDisplayFahrenheit: isSet(object.environmentDisplayFahrenheit)
        ? globalThis.Boolean(object.environmentDisplayFahrenheit)
        : false,
      airQualityEnabled: isSet(object.airQualityEnabled) ? globalThis.Boolean(object.airQualityEnabled) : false,
      airQualityInterval: isSet(object.airQualityInterval) ? globalThis.Number(object.airQualityInterval) : 0,
      powerMeasurementEnabled: isSet(object.powerMeasurementEnabled)
        ? globalThis.Boolean(object.powerMeasurementEnabled)
        : false,
      powerUpdateInterval: isSet(object.powerUpdateInterval) ? globalThis.Number(object.powerUpdateInterval) : 0,
      powerScreenEnabled: isSet(object.powerScreenEnabled) ? globalThis.Boolean(object.powerScreenEnabled) : false,
      healthMeasurementEnabled: isSet(object.healthMeasurementEnabled)
        ? globalThis.Boolean(object.healthMeasurementEnabled)
        : false,
      healthUpdateInterval: isSet(object.healthUpdateInterval) ? globalThis.Number(object.healthUpdateInterval) : 0,
      healthScreenEnabled: isSet(object.healthScreenEnabled) ? globalThis.Boolean(object.healthScreenEnabled) : false,
      deviceTelemetryEnabled: isSet(object.deviceTelemetryEnabled)
        ? globalThis.Boolean(object.deviceTelemetryEnabled)
        : false,
      airQualityScreenEnabled: isSet(object.airQualityScreenEnabled)
        ? globalThis.Boolean(object.airQualityScreenEnabled)
        : false,
    };
  },

  toJSON(message: ModuleConfig_TelemetryConfig): unknown {
    const obj: any = {};
    if (message.deviceUpdateInterval !== 0) {
      obj.deviceUpdateInterval = Math.round(message.deviceUpdateInterval);
    }
    if (message.environmentUpdateInterval !== 0) {
      obj.environmentUpdateInterval = Math.round(message.environmentUpdateInterval);
    }
    if (message.environmentMeasurementEnabled !== false) {
      obj.environmentMeasurementEnabled = message.environmentMeasurementEnabled;
    }
    if (message.environmentScreenEnabled !== false) {
      obj.environmentScreenEnabled = message.environmentScreenEnabled;
    }
    if (message.environmentDisplayFahrenheit !== false) {
      obj.environmentDisplayFahrenheit = message.environmentDisplayFahrenheit;
    }
    if (message.airQualityEnabled !== false) {
      obj.airQualityEnabled = message.airQualityEnabled;
    }
    if (message.airQualityInterval !== 0) {
      obj.airQualityInterval = Math.round(message.airQualityInterval);
    }
    if (message.powerMeasurementEnabled !== false) {
      obj.powerMeasurementEnabled = message.powerMeasurementEnabled;
    }
    if (message.powerUpdateInterval !== 0) {
      obj.powerUpdateInterval = Math.round(message.powerUpdateInterval);
    }
    if (message.powerScreenEnabled !== false) {
      obj.powerScreenEnabled = message.powerScreenEnabled;
    }
    if (message.healthMeasurementEnabled !== false) {
      obj.healthMeasurementEnabled = message.healthMeasurementEnabled;
    }
    if (message.healthUpdateInterval !== 0) {
      obj.healthUpdateInterval = Math.round(message.healthUpdateInterval);
    }
    if (message.healthScreenEnabled !== false) {
      obj.healthScreenEnabled = message.healthScreenEnabled;
    }
    if (message.deviceTelemetryEnabled !== false) {
      obj.deviceTelemetryEnabled = message.deviceTelemetryEnabled;
    }
    if (message.airQualityScreenEnabled !== false) {
      obj.airQualityScreenEnabled = message.airQualityScreenEnabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_TelemetryConfig>, I>>(base?: I): ModuleConfig_TelemetryConfig {
    return ModuleConfig_TelemetryConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_TelemetryConfig>, I>>(object: I): ModuleConfig_TelemetryConfig {
    const message = createBaseModuleConfig_TelemetryConfig();
    message.deviceUpdateInterval = object.deviceUpdateInterval ?? 0;
    message.environmentUpdateInterval = object.environmentUpdateInterval ?? 0;
    message.environmentMeasurementEnabled = object.environmentMeasurementEnabled ?? false;
    message.environmentScreenEnabled = object.environmentScreenEnabled ?? false;
    message.environmentDisplayFahrenheit = object.environmentDisplayFahrenheit ?? false;
    message.airQualityEnabled = object.airQualityEnabled ?? false;
    message.airQualityInterval = object.airQualityInterval ?? 0;
    message.powerMeasurementEnabled = object.powerMeasurementEnabled ?? false;
    message.powerUpdateInterval = object.powerUpdateInterval ?? 0;
    message.powerScreenEnabled = object.powerScreenEnabled ?? false;
    message.healthMeasurementEnabled = object.healthMeasurementEnabled ?? false;
    message.healthUpdateInterval = object.healthUpdateInterval ?? 0;
    message.healthScreenEnabled = object.healthScreenEnabled ?? false;
    message.deviceTelemetryEnabled = object.deviceTelemetryEnabled ?? false;
    message.airQualityScreenEnabled = object.airQualityScreenEnabled ?? false;
    return message;
  },
};

function createBaseModuleConfig_CannedMessageConfig(): ModuleConfig_CannedMessageConfig {
  return {
    rotary1Enabled: false,
    inputbrokerPinA: 0,
    inputbrokerPinB: 0,
    inputbrokerPinPress: 0,
    inputbrokerEventCw: 0,
    inputbrokerEventCcw: 0,
    inputbrokerEventPress: 0,
    updown1Enabled: false,
    enabled: false,
    allowInputSource: "",
    sendBell: false,
  };
}

export const ModuleConfig_CannedMessageConfig = {
  encode(message: ModuleConfig_CannedMessageConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.rotary1Enabled !== false) {
      writer.uint32(8).bool(message.rotary1Enabled);
    }
    if (message.inputbrokerPinA !== 0) {
      writer.uint32(16).uint32(message.inputbrokerPinA);
    }
    if (message.inputbrokerPinB !== 0) {
      writer.uint32(24).uint32(message.inputbrokerPinB);
    }
    if (message.inputbrokerPinPress !== 0) {
      writer.uint32(32).uint32(message.inputbrokerPinPress);
    }
    if (message.inputbrokerEventCw !== 0) {
      writer.uint32(40).int32(message.inputbrokerEventCw);
    }
    if (message.inputbrokerEventCcw !== 0) {
      writer.uint32(48).int32(message.inputbrokerEventCcw);
    }
    if (message.inputbrokerEventPress !== 0) {
      writer.uint32(56).int32(message.inputbrokerEventPress);
    }
    if (message.updown1Enabled !== false) {
      writer.uint32(64).bool(message.updown1Enabled);
    }
    if (message.enabled !== false) {
      writer.uint32(72).bool(message.enabled);
    }
    if (message.allowInputSource !== "") {
      writer.uint32(82).string(message.allowInputSource);
    }
    if (message.sendBell !== false) {
      writer.uint32(88).bool(message.sendBell);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_CannedMessageConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_CannedMessageConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.rotary1Enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.inputbrokerPinA = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.inputbrokerPinB = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.inputbrokerPinPress = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.inputbrokerEventCw = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.inputbrokerEventCcw = reader.int32() as any;
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.inputbrokerEventPress = reader.int32() as any;
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.updown1Enabled = reader.bool();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.allowInputSource = reader.string();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.sendBell = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_CannedMessageConfig {
    return {
      rotary1Enabled: isSet(object.rotary1Enabled) ? globalThis.Boolean(object.rotary1Enabled) : false,
      inputbrokerPinA: isSet(object.inputbrokerPinA) ? globalThis.Number(object.inputbrokerPinA) : 0,
      inputbrokerPinB: isSet(object.inputbrokerPinB) ? globalThis.Number(object.inputbrokerPinB) : 0,
      inputbrokerPinPress: isSet(object.inputbrokerPinPress) ? globalThis.Number(object.inputbrokerPinPress) : 0,
      inputbrokerEventCw: isSet(object.inputbrokerEventCw)
        ? moduleConfig_CannedMessageConfig_InputEventCharFromJSON(object.inputbrokerEventCw)
        : 0,
      inputbrokerEventCcw: isSet(object.inputbrokerEventCcw)
        ? moduleConfig_CannedMessageConfig_InputEventCharFromJSON(object.inputbrokerEventCcw)
        : 0,
      inputbrokerEventPress: isSet(object.inputbrokerEventPress)
        ? moduleConfig_CannedMessageConfig_InputEventCharFromJSON(object.inputbrokerEventPress)
        : 0,
      updown1Enabled: isSet(object.updown1Enabled) ? globalThis.Boolean(object.updown1Enabled) : false,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      allowInputSource: isSet(object.allowInputSource) ? globalThis.String(object.allowInputSource) : "",
      sendBell: isSet(object.sendBell) ? globalThis.Boolean(object.sendBell) : false,
    };
  },

  toJSON(message: ModuleConfig_CannedMessageConfig): unknown {
    const obj: any = {};
    if (message.rotary1Enabled !== false) {
      obj.rotary1Enabled = message.rotary1Enabled;
    }
    if (message.inputbrokerPinA !== 0) {
      obj.inputbrokerPinA = Math.round(message.inputbrokerPinA);
    }
    if (message.inputbrokerPinB !== 0) {
      obj.inputbrokerPinB = Math.round(message.inputbrokerPinB);
    }
    if (message.inputbrokerPinPress !== 0) {
      obj.inputbrokerPinPress = Math.round(message.inputbrokerPinPress);
    }
    if (message.inputbrokerEventCw !== 0) {
      obj.inputbrokerEventCw = moduleConfig_CannedMessageConfig_InputEventCharToJSON(message.inputbrokerEventCw);
    }
    if (message.inputbrokerEventCcw !== 0) {
      obj.inputbrokerEventCcw = moduleConfig_CannedMessageConfig_InputEventCharToJSON(message.inputbrokerEventCcw);
    }
    if (message.inputbrokerEventPress !== 0) {
      obj.inputbrokerEventPress = moduleConfig_CannedMessageConfig_InputEventCharToJSON(message.inputbrokerEventPress);
    }
    if (message.updown1Enabled !== false) {
      obj.updown1Enabled = message.updown1Enabled;
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.allowInputSource !== "") {
      obj.allowInputSource = message.allowInputSource;
    }
    if (message.sendBell !== false) {
      obj.sendBell = message.sendBell;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_CannedMessageConfig>, I>>(
    base?: I,
  ): ModuleConfig_CannedMessageConfig {
    return ModuleConfig_CannedMessageConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_CannedMessageConfig>, I>>(
    object: I,
  ): ModuleConfig_CannedMessageConfig {
    const message = createBaseModuleConfig_CannedMessageConfig();
    message.rotary1Enabled = object.rotary1Enabled ?? false;
    message.inputbrokerPinA = object.inputbrokerPinA ?? 0;
    message.inputbrokerPinB = object.inputbrokerPinB ?? 0;
    message.inputbrokerPinPress = object.inputbrokerPinPress ?? 0;
    message.inputbrokerEventCw = object.inputbrokerEventCw ?? 0;
    message.inputbrokerEventCcw = object.inputbrokerEventCcw ?? 0;
    message.inputbrokerEventPress = object.inputbrokerEventPress ?? 0;
    message.updown1Enabled = object.updown1Enabled ?? false;
    message.enabled = object.enabled ?? false;
    message.allowInputSource = object.allowInputSource ?? "";
    message.sendBell = object.sendBell ?? false;
    return message;
  },
};

function createBaseModuleConfig_AmbientLightingConfig(): ModuleConfig_AmbientLightingConfig {
  return { ledState: false, current: 0, red: 0, green: 0, blue: 0 };
}

export const ModuleConfig_AmbientLightingConfig = {
  encode(message: ModuleConfig_AmbientLightingConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ledState !== false) {
      writer.uint32(8).bool(message.ledState);
    }
    if (message.current !== 0) {
      writer.uint32(16).uint32(message.current);
    }
    if (message.red !== 0) {
      writer.uint32(24).uint32(message.red);
    }
    if (message.green !== 0) {
      writer.uint32(32).uint32(message.green);
    }
    if (message.blue !== 0) {
      writer.uint32(40).uint32(message.blue);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_AmbientLightingConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_AmbientLightingConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.ledState = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.current = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.red = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.green = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.blue = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_AmbientLightingConfig {
    return {
      ledState: isSet(object.ledState) ? globalThis.Boolean(object.ledState) : false,
      current: isSet(object.current) ? globalThis.Number(object.current) : 0,
      red: isSet(object.red) ? globalThis.Number(object.red) : 0,
      green: isSet(object.green) ? globalThis.Number(object.green) : 0,
      blue: isSet(object.blue) ? globalThis.Number(object.blue) : 0,
    };
  },

  toJSON(message: ModuleConfig_AmbientLightingConfig): unknown {
    const obj: any = {};
    if (message.ledState !== false) {
      obj.ledState = message.ledState;
    }
    if (message.current !== 0) {
      obj.current = Math.round(message.current);
    }
    if (message.red !== 0) {
      obj.red = Math.round(message.red);
    }
    if (message.green !== 0) {
      obj.green = Math.round(message.green);
    }
    if (message.blue !== 0) {
      obj.blue = Math.round(message.blue);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_AmbientLightingConfig>, I>>(
    base?: I,
  ): ModuleConfig_AmbientLightingConfig {
    return ModuleConfig_AmbientLightingConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_AmbientLightingConfig>, I>>(
    object: I,
  ): ModuleConfig_AmbientLightingConfig {
    const message = createBaseModuleConfig_AmbientLightingConfig();
    message.ledState = object.ledState ?? false;
    message.current = object.current ?? 0;
    message.red = object.red ?? 0;
    message.green = object.green ?? 0;
    message.blue = object.blue ?? 0;
    return message;
  },
};

function createBaseModuleConfig_StatusMessageConfig(): ModuleConfig_StatusMessageConfig {
  return { nodeStatus: "" };
}

export const ModuleConfig_StatusMessageConfig = {
  encode(message: ModuleConfig_StatusMessageConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.nodeStatus !== "") {
      writer.uint32(10).string(message.nodeStatus);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_StatusMessageConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_StatusMessageConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.nodeStatus = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_StatusMessageConfig {
    return { nodeStatus: isSet(object.nodeStatus) ? globalThis.String(object.nodeStatus) : "" };
  },

  toJSON(message: ModuleConfig_StatusMessageConfig): unknown {
    const obj: any = {};
    if (message.nodeStatus !== "") {
      obj.nodeStatus = message.nodeStatus;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_StatusMessageConfig>, I>>(
    base?: I,
  ): ModuleConfig_StatusMessageConfig {
    return ModuleConfig_StatusMessageConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_StatusMessageConfig>, I>>(
    object: I,
  ): ModuleConfig_StatusMessageConfig {
    const message = createBaseModuleConfig_StatusMessageConfig();
    message.nodeStatus = object.nodeStatus ?? "";
    return message;
  },
};

function createBaseModuleConfig_MeshBeaconConfig(): ModuleConfig_MeshBeaconConfig {
  return {
    flags: 0,
    broadcastSendAsNode: 0,
    broadcastMessage: "",
    broadcastOfferChannel: undefined,
    broadcastOfferRegion: 0,
    broadcastOfferPreset: undefined,
    broadcastOnChannel: undefined,
    broadcastOnRegion: 0,
    broadcastOnPreset: undefined,
    broadcastIntervalSecs: 0,
    broadcastTargets: [],
  };
}

export const ModuleConfig_MeshBeaconConfig = {
  encode(message: ModuleConfig_MeshBeaconConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.flags !== 0) {
      writer.uint32(8).uint32(message.flags);
    }
    if (message.broadcastSendAsNode !== 0) {
      writer.uint32(24).uint32(message.broadcastSendAsNode);
    }
    if (message.broadcastMessage !== "") {
      writer.uint32(34).string(message.broadcastMessage);
    }
    if (message.broadcastOfferChannel !== undefined) {
      ChannelSettings.encode(message.broadcastOfferChannel, writer.uint32(42).fork()).ldelim();
    }
    if (message.broadcastOfferRegion !== 0) {
      writer.uint32(48).int32(message.broadcastOfferRegion);
    }
    if (message.broadcastOfferPreset !== undefined) {
      writer.uint32(56).int32(message.broadcastOfferPreset);
    }
    if (message.broadcastOnChannel !== undefined) {
      ChannelSettings.encode(message.broadcastOnChannel, writer.uint32(66).fork()).ldelim();
    }
    if (message.broadcastOnRegion !== 0) {
      writer.uint32(72).int32(message.broadcastOnRegion);
    }
    if (message.broadcastOnPreset !== undefined) {
      writer.uint32(80).int32(message.broadcastOnPreset);
    }
    if (message.broadcastIntervalSecs !== 0) {
      writer.uint32(88).uint32(message.broadcastIntervalSecs);
    }
    for (const v of message.broadcastTargets) {
      ModuleConfig_MeshBeaconConfig_BroadcastTarget.encode(v!, writer.uint32(106).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_MeshBeaconConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_MeshBeaconConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.flags = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.broadcastSendAsNode = reader.uint32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.broadcastMessage = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.broadcastOfferChannel = ChannelSettings.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.broadcastOfferRegion = reader.int32() as any;
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.broadcastOfferPreset = reader.int32() as any;
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.broadcastOnChannel = ChannelSettings.decode(reader, reader.uint32());
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.broadcastOnRegion = reader.int32() as any;
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.broadcastOnPreset = reader.int32() as any;
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.broadcastIntervalSecs = reader.uint32();
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.broadcastTargets.push(ModuleConfig_MeshBeaconConfig_BroadcastTarget.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_MeshBeaconConfig {
    return {
      flags: isSet(object.flags) ? globalThis.Number(object.flags) : 0,
      broadcastSendAsNode: isSet(object.broadcastSendAsNode) ? globalThis.Number(object.broadcastSendAsNode) : 0,
      broadcastMessage: isSet(object.broadcastMessage) ? globalThis.String(object.broadcastMessage) : "",
      broadcastOfferChannel: isSet(object.broadcastOfferChannel)
        ? ChannelSettings.fromJSON(object.broadcastOfferChannel)
        : undefined,
      broadcastOfferRegion: isSet(object.broadcastOfferRegion)
        ? config_LoRaConfig_RegionCodeFromJSON(object.broadcastOfferRegion)
        : 0,
      broadcastOfferPreset: isSet(object.broadcastOfferPreset)
        ? config_LoRaConfig_ModemPresetFromJSON(object.broadcastOfferPreset)
        : undefined,
      broadcastOnChannel: isSet(object.broadcastOnChannel)
        ? ChannelSettings.fromJSON(object.broadcastOnChannel)
        : undefined,
      broadcastOnRegion: isSet(object.broadcastOnRegion)
        ? config_LoRaConfig_RegionCodeFromJSON(object.broadcastOnRegion)
        : 0,
      broadcastOnPreset: isSet(object.broadcastOnPreset)
        ? config_LoRaConfig_ModemPresetFromJSON(object.broadcastOnPreset)
        : undefined,
      broadcastIntervalSecs: isSet(object.broadcastIntervalSecs) ? globalThis.Number(object.broadcastIntervalSecs) : 0,
      broadcastTargets: globalThis.Array.isArray(object?.broadcastTargets)
        ? object.broadcastTargets.map((e: any) => ModuleConfig_MeshBeaconConfig_BroadcastTarget.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ModuleConfig_MeshBeaconConfig): unknown {
    const obj: any = {};
    if (message.flags !== 0) {
      obj.flags = Math.round(message.flags);
    }
    if (message.broadcastSendAsNode !== 0) {
      obj.broadcastSendAsNode = Math.round(message.broadcastSendAsNode);
    }
    if (message.broadcastMessage !== "") {
      obj.broadcastMessage = message.broadcastMessage;
    }
    if (message.broadcastOfferChannel !== undefined) {
      obj.broadcastOfferChannel = ChannelSettings.toJSON(message.broadcastOfferChannel);
    }
    if (message.broadcastOfferRegion !== 0) {
      obj.broadcastOfferRegion = config_LoRaConfig_RegionCodeToJSON(message.broadcastOfferRegion);
    }
    if (message.broadcastOfferPreset !== undefined) {
      obj.broadcastOfferPreset = config_LoRaConfig_ModemPresetToJSON(message.broadcastOfferPreset);
    }
    if (message.broadcastOnChannel !== undefined) {
      obj.broadcastOnChannel = ChannelSettings.toJSON(message.broadcastOnChannel);
    }
    if (message.broadcastOnRegion !== 0) {
      obj.broadcastOnRegion = config_LoRaConfig_RegionCodeToJSON(message.broadcastOnRegion);
    }
    if (message.broadcastOnPreset !== undefined) {
      obj.broadcastOnPreset = config_LoRaConfig_ModemPresetToJSON(message.broadcastOnPreset);
    }
    if (message.broadcastIntervalSecs !== 0) {
      obj.broadcastIntervalSecs = Math.round(message.broadcastIntervalSecs);
    }
    if (message.broadcastTargets?.length) {
      obj.broadcastTargets = message.broadcastTargets.map((e) =>
        ModuleConfig_MeshBeaconConfig_BroadcastTarget.toJSON(e)
      );
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_MeshBeaconConfig>, I>>(base?: I): ModuleConfig_MeshBeaconConfig {
    return ModuleConfig_MeshBeaconConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_MeshBeaconConfig>, I>>(
    object: I,
  ): ModuleConfig_MeshBeaconConfig {
    const message = createBaseModuleConfig_MeshBeaconConfig();
    message.flags = object.flags ?? 0;
    message.broadcastSendAsNode = object.broadcastSendAsNode ?? 0;
    message.broadcastMessage = object.broadcastMessage ?? "";
    message.broadcastOfferChannel =
      (object.broadcastOfferChannel !== undefined && object.broadcastOfferChannel !== null)
        ? ChannelSettings.fromPartial(object.broadcastOfferChannel)
        : undefined;
    message.broadcastOfferRegion = object.broadcastOfferRegion ?? 0;
    message.broadcastOfferPreset = object.broadcastOfferPreset ?? undefined;
    message.broadcastOnChannel = (object.broadcastOnChannel !== undefined && object.broadcastOnChannel !== null)
      ? ChannelSettings.fromPartial(object.broadcastOnChannel)
      : undefined;
    message.broadcastOnRegion = object.broadcastOnRegion ?? 0;
    message.broadcastOnPreset = object.broadcastOnPreset ?? undefined;
    message.broadcastIntervalSecs = object.broadcastIntervalSecs ?? 0;
    message.broadcastTargets =
      object.broadcastTargets?.map((e) => ModuleConfig_MeshBeaconConfig_BroadcastTarget.fromPartial(e)) || [];
    return message;
  },
};

function createBaseModuleConfig_MeshBeaconConfig_BroadcastTarget(): ModuleConfig_MeshBeaconConfig_BroadcastTarget {
  return { preset: undefined, region: 0, channelIndex: undefined };
}

export const ModuleConfig_MeshBeaconConfig_BroadcastTarget = {
  encode(message: ModuleConfig_MeshBeaconConfig_BroadcastTarget, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.preset !== undefined) {
      writer.uint32(8).int32(message.preset);
    }
    if (message.region !== 0) {
      writer.uint32(16).int32(message.region);
    }
    if (message.channelIndex !== undefined) {
      writer.uint32(32).uint32(message.channelIndex);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_MeshBeaconConfig_BroadcastTarget {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_MeshBeaconConfig_BroadcastTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.preset = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.region = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.channelIndex = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_MeshBeaconConfig_BroadcastTarget {
    return {
      preset: isSet(object.preset) ? config_LoRaConfig_ModemPresetFromJSON(object.preset) : undefined,
      region: isSet(object.region) ? config_LoRaConfig_RegionCodeFromJSON(object.region) : 0,
      channelIndex: isSet(object.channelIndex) ? globalThis.Number(object.channelIndex) : undefined,
    };
  },

  toJSON(message: ModuleConfig_MeshBeaconConfig_BroadcastTarget): unknown {
    const obj: any = {};
    if (message.preset !== undefined) {
      obj.preset = config_LoRaConfig_ModemPresetToJSON(message.preset);
    }
    if (message.region !== 0) {
      obj.region = config_LoRaConfig_RegionCodeToJSON(message.region);
    }
    if (message.channelIndex !== undefined) {
      obj.channelIndex = Math.round(message.channelIndex);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_MeshBeaconConfig_BroadcastTarget>, I>>(
    base?: I,
  ): ModuleConfig_MeshBeaconConfig_BroadcastTarget {
    return ModuleConfig_MeshBeaconConfig_BroadcastTarget.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_MeshBeaconConfig_BroadcastTarget>, I>>(
    object: I,
  ): ModuleConfig_MeshBeaconConfig_BroadcastTarget {
    const message = createBaseModuleConfig_MeshBeaconConfig_BroadcastTarget();
    message.preset = object.preset ?? undefined;
    message.region = object.region ?? 0;
    message.channelIndex = object.channelIndex ?? undefined;
    return message;
  },
};

function createBaseModuleConfig_TAKConfig(): ModuleConfig_TAKConfig {
  return { team: 0, role: 0 };
}

export const ModuleConfig_TAKConfig = {
  encode(message: ModuleConfig_TAKConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.team !== 0) {
      writer.uint32(8).int32(message.team);
    }
    if (message.role !== 0) {
      writer.uint32(16).int32(message.role);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ModuleConfig_TAKConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleConfig_TAKConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.team = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleConfig_TAKConfig {
    return {
      team: isSet(object.team) ? teamFromJSON(object.team) : 0,
      role: isSet(object.role) ? memberRoleFromJSON(object.role) : 0,
    };
  },

  toJSON(message: ModuleConfig_TAKConfig): unknown {
    const obj: any = {};
    if (message.team !== 0) {
      obj.team = teamToJSON(message.team);
    }
    if (message.role !== 0) {
      obj.role = memberRoleToJSON(message.role);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleConfig_TAKConfig>, I>>(base?: I): ModuleConfig_TAKConfig {
    return ModuleConfig_TAKConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleConfig_TAKConfig>, I>>(object: I): ModuleConfig_TAKConfig {
    const message = createBaseModuleConfig_TAKConfig();
    message.team = object.team ?? 0;
    message.role = object.role ?? 0;
    return message;
  },
};

function createBaseRemoteHardwarePin(): RemoteHardwarePin {
  return { gpioPin: 0, name: "", type: 0 };
}

export const RemoteHardwarePin = {
  encode(message: RemoteHardwarePin, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gpioPin !== 0) {
      writer.uint32(8).uint32(message.gpioPin);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RemoteHardwarePin {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoteHardwarePin();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.gpioPin = reader.uint32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoteHardwarePin {
    return {
      gpioPin: isSet(object.gpioPin) ? globalThis.Number(object.gpioPin) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? remoteHardwarePinTypeFromJSON(object.type) : 0,
    };
  },

  toJSON(message: RemoteHardwarePin): unknown {
    const obj: any = {};
    if (message.gpioPin !== 0) {
      obj.gpioPin = Math.round(message.gpioPin);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== 0) {
      obj.type = remoteHardwarePinTypeToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoteHardwarePin>, I>>(base?: I): RemoteHardwarePin {
    return RemoteHardwarePin.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoteHardwarePin>, I>>(object: I): RemoteHardwarePin {
    const message = createBaseRemoteHardwarePin();
    message.gpioPin = object.gpioPin ?? 0;
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
